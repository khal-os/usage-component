## What & why

<!-- One or two sentences. Link the story (T<n>/US<n>) or decision if one applies. -->

## Checklist

- [ ] PR **title** is a conventional commit — it becomes the squash commit on `main`
- [ ] New decisions made here are in the decision log (`docs/produto/backlog-v2.3.md`)
- [ ] Test suffixes are right: `*.spec.ts` = unit (no Mongo), `*.test.ts` = integration (real Mongo)
- [ ] No invariant from `CLAUDE.md` is violated (price immutability, R$-only projections, one truth for billing)

## Anything a reviewer should know

<!-- Risky spots, follow-ups deferred, how you verified it. Delete if nothing. -->
