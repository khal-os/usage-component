# Plataforma de Agentes de IA — Backlog Consolidado v2.3
### Uma API, três abas: **Billing · Traces · Sessions**
Discovery: jul/2026 · v2.3 — versão de trabalho alinhada à planilha Traces_Sessions_Billing-v2.3: carimbo de preço na ingestão; cortes US9/US13/US14/US16; Reconciliação dobrada em T6/T7/US7

---

## 0. A moldura do produto

**Visão.** Uma plataforma single-tenant (uma instância por cliente) onde o cliente enxerga, valida e entende seus agentes de IA. A lógica das três abas conta uma história só: **Billing** diz quanto custou, **Traces** mostra as execuções reais que geraram esse custo, e **Sessions** mostra as conversas às quais essas execuções pertencem. Uma API, um armazenamento, uma verdade — tudo alimentado pelo LangWatch, que é o conector entre os agentes e a plataforma.

**Persona principal.** O gestor do lado do cliente. Os objetivos dele, em ordem de prioridade: (1) validar a fatura, (2) controlar e projetar o gasto, (3) entender o que compõe o custo, (4) prestar contas internamente. Nas abas de Traces e Sessions, o trabalho principal é **transparência e confiança**: ver as execuções e conversas por trás dos números. Depuração e revisão de qualidade são beneficiários secundários, não o motivo da aba existir.

**Persona de apoio.** O admin da plataforma (lado Khal): mantém os preços (direto no banco, na v1), acompanha a saúde da ingestão, fecha os meses e gera a fatura real a partir do extrato congelado.

**A arquitetura em uma linha.** API do LangWatch → sync em nível de trace → **precificação no ato da gravação** (cada trace entra carimbado com o preço contratado em R$ vigente na sua data; o carimbo é imutável) → armazenamento próprio (traces + spans + conteúdo completo; é o arquivo permanente, já que o LangWatch guarda só ~49 dias) → snapshots mensais (billing) + visões ao vivo (traces/sessions) → três abas de UI.

**Escopo da v1.** Só custo de tokens · agentes de texto apenas · conteúdo das conversas exibido na íntegra, sem mascaramento · single-tenant · domínio e subdomínio como strings simples · preços em R$ como dados versionados (markup, US$ e PTAX são internos e nunca chegam ao cliente) · faturamento por mês-calendário · meses fechados congelados em snapshots completos de auditoria.

**Fora da v1.** Custo de infra/compute · papéis e hierarquia de RBAC · canal de voz · política de mascaramento e retenção (a postura da v1 é explícita: guarda tudo, para sempre) · telas de admin para preço/reclassificação/reabertura (o motor existe, a operação é via runbook) · alertas e notificações · fluxo de contestação.

**Princípio do backlog.** Quando adiamos uma história de usuário, estamos adiando só a camada de interação. As tech stories que a sustentam — o motor — são construídas na v1. Assim, a tela futura vira só uma janela para um comportamento que já funciona.

**Ordem de construção.** A ordem dos épicos segue a cadeia de derivação dos dados — o trace é o átomo; sessão é trace agrupado; billing é trace precificado e congelado: Épico 1 (Fundação: sync e armazenamento de traces) → 2 (Preços e custo por trace) → 3 (Aba Traces) → 4 (Aba Sessions) → 5 (Fechamento do mês) → 6 (Validação da fatura) → 7 (Tendências) → 8 (Composição) → 9 (Exportação) → 10 (Reconciliação, cujo check de mês aberto pode ligar assim que o billing ao vivo existir). Trade-off consciente e registrado (decisão 24): a validação da fatura — objetivo nº 1 do gestor — chega depois das abas Traces/Sessions; em compensação, nenhum mês fecha antes de estar integralmente ingerido, e a transparência das execuções entrega valor desde cedo. **Os IDs de histórias (T1–T12, US1–US25) permanecem os mesmos da v2** — só a ordem e a numeração dos épicos mudaram.

---

## Épico 1 — Preços e Contratos

**Objetivo.** Os preços contratados do cliente, em R$ (por modelo × tipo de token), guardados como dados versionados — mantidos direto no banco na v1. Este épico vem **antes de tudo** por uma razão de arquitetura: a ingestão **carimba o preço no trace no ato da gravação**; sem tabela de preços de pé, o pipeline não roda. O cliente nunca vê markup, US$ nem PTAX.

### T4 — Modelo de dados de preços versionado (mantido via banco na v1)
- Registro de preço = modelo + tipo de token + valor em R$ por milhão de tokens + vigência-de (a vigência-até deriva da versão seguinte). Single-tenant: uma instância por cliente, então não precisa de chave de cliente — mas vale manter o schema honesto se o código for compartilhado entre instâncias.
- Lookup **as-of** determinístico para qualquer data de uso.
- Versões imutáveis: mudança é insert novo, nunca update em cima.
- Constraints do banco barram duplicidade (modelo, tipo de token, vigência-de) e vigências dentro de mês fechado.
- A lista de modelos é dado, não código — modelo novo entra sem deploy.
- Colunas internas opcionais por versão (custo de mercado em US$, PTAX de referência, markup %) — não entram no cálculo da v1, invisíveis ao cliente, mas capturadas desde já para a futura análise de margem. *[**Superado pela decisão 96**: as colunas soltas foram removidas — `pricingType` é a costura para margem/preço computado; cada tipo de precificação declara os SEUS campos na própria versão.]*
- Runbook: o procedimento/SQL canônico para mexer em preço.
*Habilita: a precificação no ato da ingestão (T5, Épico 2), US3, US4 e todos os épicos seguintes; futuras telas de preço e histórico.*

### US3 — Alerta de uso sem preço (admin, mínimo)
*Como admin da plataforma, quero ser avisado quando entrar uso sem preço aplicável (modelo desconhecido ou versão faltando), para que nenhum consumo seja cobrado a zero em silêncio, nem se perca.*
- Traces ingeridos sem preço aplicável ficam como 'preço pendente' (com volume, período e modelo identificáveis); o aviso aparece onde o admin já olha — sem tela dedicada.
- Nunca aparece como R$ 0,00 para o cliente; ou se resolve — registra-se o preço e os traces pendentes são carimbados (só em mês aberto) — ou se exclui explicitamente antes do fechamento.

### US4 — Gestor vê os preços contratados
*Como gestor do cliente, quero ver a tabela de preços aplicada à minha empresa (R$ por milhão de tokens, por modelo e tipo de token), para conferir a conta da fatura contra o meu contrato.*
- Somente leitura; a versão aplicada ao período em tela (quando o fechamento de mês existir — Épico 5 —, mês fechado renderiza a partir do snapshot; até lá e no mês corrente, mostra a versão vigente).
- Só os preços finais em R$ — nada de US$, PTAX, markup ou campo interno, em lugar nenhum.

**Adiado:** tela de cadastro/alteração de preços · visão de histórico · análise de margem · construções contratuais (mínimos, faixas de volume, taxas fixas).

---

## Épico 2 — Fundação: contrato de metadados, ingestão com precificação no ato, e armazenamento

**Objetivo.** Traces completos (métricas, spans e o conteúdo integral das conversas) fluem do LangWatch para o armazenamento próprio dentro da janela de 49 dias — e **já entram precificados**: no ato da gravação, cada trace recebe o carimbo do preço vigente e do custo resultante, que se torna imutável dali em diante. Os agregados do billing são somas desses custos carimbados — o insumo do faturamento, por construção.

### T1 — Contrato de metadados (um contrato, três consumidores)
Definir e documentar o contrato de metadados dos traces do LangWatch, atendendo billing, traces e sessions de uma vez.
- Campos do trace: ID do trace, **ID da sessão**, ID do agente, modelo(s), timestamps de início/fim, status (ok/erro), contagem de tokens por tipo (input / output / cache leitura / cache escrita), **canal** (whatsapp/web/… — deixa o terreno pronto para voz), **domínio e subdomínio como strings opcionais**, tipo do trace.
- Campos do span: ID, trace pai, tipo (llm, tool, retrieval, guardrail, …), nome (ex.: nome da ferramenta — nomes crus servem na v1), início/fim, status, mensagem de erro quando falha, tokens quando aplicável.
- Conteúdo: payloads completos de entrada e saída por trace e pelos spans relevantes (sem mascaramento — decisão da v1).
- **Matriz campo × consumidor** (billing / traces / sessions): quem precisa de quê; obrigatório vs. opcional, com comportamento de fallback. A atribuição de segmento do billing (system prompt / tool call / conversa) se deriva dos spans — uma instrumentação serve às duas visões.
- Versionado; acordado com os times dos agentes. **É a dependência que trava a fase de modelagem.**
*Habilita: tudo.*

### T2 — Sync em nível de trace, com detecção de buracos e proteção da janela de retenção
Job agendado que ingere traces completos (conforme T1) da API do LangWatch.
- Agendado, incremental, **idempotente** (reexecuções e janelas sobrepostas nunca contam em dobro), seguro contra falhas (escrita parcial nunca conta como completa), toda execução logada (janela, registros, status).
- Ingere o trace inteiro: métricas, spans, conteúdo — e aciona a precificação no ato (T5): o trace já é gravado com o custo carimbado (ou como preço pendente).
- **Proteção da janela de retenção:** alertas escalam conforme dados não sincronizados envelhecem rumo ao limite de 49 dias do LangWatch; o monitoramento expõe o timestamp mais antigo ainda não sincronizado. Uma pane no sync tem prazo fatal — isso aqui é prevenção de perda de dados, não higiene.
- **Detecção de buracos:** continuidade verificável; lacunas detectadas e reportadas; uma rodada de reparo rebusca qualquer janela que ainda esteja dentro da retenção.
- **Backfill de largada:** a primeira execução ingere tudo que o LangWatch ainda guarda (~49 dias) — esse é o histórico inaugural da plataforma. Rodar o quanto antes: cada semana de atraso é história perdida para sempre.
*Habilita: US1, US2, todas as abas. (Depende também do T4 — sem tabela de preços não há carimbo.)*

### T3 — Armazenamento de traces e agregados derivados
Modelo de armazenamento: traces, spans e conteúdo no banco próprio; agregados do billing derivados dali.
- Entidades: trace (todos os campos do T1) → spans ordenados e cronometrados → blocos de conteúdo; índices pensados para as consultas das abas (período + agente + status + domínio/subdomínio + tipo + busca por ID).
- **Os agregados diários do billing (agente × modelo × tipo de token × segmento × dia) são somas dos custos carimbados nos traces armazenados** — uma fonte só, uma precificação só.
- Sessão é um read-model derivado: agrupamento dos traces por ID de sessão (materializado ou calculado — decisão de implementação).
- Traces com metadados ausentes ou inválidos são armazenados e marcados como "não classificados", com o motivo — nunca descartados (sem tela na v1).
- Correções de atribuição/metadados continuam possíveis em períodos abertos, com reagregação em cascata — o carimbo de preço não muda (T5); a tela fica para depois.
- O armazenamento é o arquivo permanente: nada de expurgo automático na v1; política de retenção é uma decisão adiada e registrada.
*Habilita: US1, US2, todos os épicos seguintes e a futura tela de reclassificação.*

### T5 — Precificação no ato da ingestão (price stamping)
No momento em que o trace é gravado, o pipeline resolve o preço e **carimba** o custo no registro. O carimbo é definitivo.
- Regra do carimbo: usa-se a versão de preço **vigente na data do trace**, resolvida no ato da ingestão (as-of na escrita — confirmar na QA19). *[**Confirmada pela decisão 138 (09/08/2026)**: a proposta virou regra]* Carimbam-se, por tipo de token: preço aplicado (R$/milhão) e custo resultante; o custo total do trace é a soma.
- **Imutável depois de gravado:** mudança de preço posterior vale só para traces ingeridos dali em diante — nunca reprecifica trace consolidado. A imutabilidade sai do fechamento do mês e passa para a escrita.
- Trace ingerido sem preço aplicável entra como **'preço pendente'** (tokens guardados, custo em aberto, fora dos totais em R$ — nunca valorado a zero); ao registrar o preço (só em mês aberto), os pendentes são carimbados. A US3 vigia essa fila.
- Carimbo errado só se corrige pelo fluxo auditado de reabertura (T6) — sem recálculo silencioso.
- Determinístico e reproduzível ao centavo; regra de arredondamento definida e documentada (precisão cheia na linha; arredondar só totais exibidos/faturados, half-up 2 casas); as partes exibidas fecham com o total exibido.
- Correção de **atribuição** (agente/metadados, período aberto) segue possível e não altera o carimbo — o preço é por modelo, não por agente.
*Habilita: o custo por trace das abas Traces/Sessions e, adiante, os agregados e snapshots do billing.*

### US1 — Visibilidade da saúde do sync e do risco de perda de dados (admin)
*Como admin da plataforma, quero enxergar a saúde da ingestão — último sync bem-sucedido, dado mais antigo não sincronizado, buracos detectados — para agir antes que a janela de 49 dias transforme um atraso em perda definitiva.*
- A saúde aparece onde o admin já olha (endpoint de status / log / alerta — sem tela dedicada na v1).
- Alerta em limiar de atenção (~35 dias) e crítico (~45 dias) de idade do dado não sincronizado, e em qualquer buraco detectado.

### US2 — Visibilidade da atualização dos dados (gestor)
*Como gestor do cliente, quero saber quando os dados que estou vendo foram atualizados pela última vez, para nunca confundir dado velho ou parcial com a realidade.*
- Toda visão do gestor mostra "dados atualizados em [timestamp]", lendo do mesmo status da US1 — uma verdade só sobre atualização.
- Se o sync falhar ou atrasar, o admin é alertado; o gestor continua vendo o timestamp honesto.

**Adiado:** fluxo de revisão de agentes novos (o motor já ingere agentes desconhecidos — T3) · tela de gestão de uso não classificado (armazenado e marcado, atribuição mutável — T3) · política de retenção/mascaramento (LGPD) · voz (chega como novo canal + novos tipos de span, sem migração).

---

## Épico 3 — Aba Traces

**Objetivo.** O gestor vê as execuções reais por trás dos números: uma lista filtrável de traces com custo por execução, e um drawer com a anatomia completa de qualquer uma — métricas, cascata de spans e o conteúdo da conversa. Trabalho principal: transparência e confiança.

### T10 — API de consulta de traces
Endpoints que servem a aba a partir do armazenamento próprio.
- Endpoint de listagem: filtros de período (intervalo de datas), agente, status (todos/ok/erro), tipo de trace, domínio/subdomínio (match de string), busca livre por ID de trace/sessão; paginação e ordenação no servidor (mais recente primeiro, por padrão).
- Custo por trace = **o valor carimbado na ingestão** (T5) — o mesmo número que alimenta os agregados do billing, por construção.
- Endpoint de detalhe: o trace inteiro — métricas, spans ordenados com tempo/status/erro, conteúdo de entrada e saída.
- Meta de desempenho: listagem paginada e indexada para o volume realista do armazenamento (dimensionamento na QA15).
*Habilita: US18–US20 e o épico de Reconciliação.*

### US18 — Lista de traces com filtros
*Como gestor do cliente, quero navegar e filtrar as execuções dos meus agentes (por período, agente, status, tipo, domínio/subdomínio), com o custo de cada uma, para ver a atividade concreta pela qual estou pagando.*
- Colunas (o mock é a referência): ID do trace (mono), agente (+ caminho domínio/subdomínio quando houver), tag de tipo, pill de status, duração, tokens (in+out), **custo em R$**, quando (relativo se for hoje; dd/mm hh:mm antes disso).
- Filtros combináveis; estados vazios honestos ("nenhum trace neste período/filtro").
- Honestidade sobre frescor desde já: o dado é tão fresco quanto o último sync (timestamp da US2 à vista); quando o ciclo de fechamento existir (Épico 5), a US6 soma o rótulo fechado/em andamento.

### US19 — Drawer do trace: a anatomia de uma execução
*Como gestor do cliente, quero abrir qualquer trace e ver sua anatomia completa — métricas, a cascata passo a passo e o conteúdo real da conversa — para entender exatamente o que é uma execução e quanto ela custou.*
- Cabeçalho: agente + caminho, chip de canal, timestamp completo + relativo, modelo servido, **link para a sessão dele**.
- Linha de métricas: duração, tokens in, tokens out, custo (R$).
- **Cascata de spans:** barras posicionadas no tempo, cor por tipo de span, span que falhou em destaque; nomes visíveis (crus mesmo); expandir/passar o mouse mostra tempo e status do span.
- **Conteúdo:** a entrada e a saída do trace na íntegra (sem mascaramento — decisão da v1); em erro, a mensagem do span que falhou.
- Tudo vem do armazenamento próprio (autocontido — sem dependência do LangWatch na hora de exibir).

### US20 — Deeplink trace → sessão
*Como gestor do cliente, quero pular de um trace para a sessão (conversa) a que ele pertence, para ver a execução no seu contexto conversacional.*
- O link do drawer abre a visão da sessão (Épico 4) ancorada nela; trace sem ID de sessão mostra "sem sessão", com honestidade.

**Adiado:** filtros salvos · exportação da lista em CSV · exibição de custo por span (o motor tem os tokens por span onde houver instrumentação).

---

## Épico 4 — Aba Sessions

**Objetivo.** Conversas como objetos de primeira classe: traces agrupados em sessões, com métricas agregadas e uma visão de conversa legível. É aqui que mora o "ler o que o agente realmente disse" — a camada mais profunda de confiança.

### T11 — Read-model de sessões e agregados
- Sessões derivadas dos traces armazenados, agrupados por ID de sessão; agregados por sessão: nº de traces, status ("com erro" se qualquer trace falhou), duração somada, tokens somados, **custo somado (soma dos custos dos traces — fecha por construção)**, início (primeiro trace), última atividade.
- Uma sessão está "viva" enquanto traces novos ainda podem chegar — sem flag artificial de "encerrada" na v1; ordenação cronológica pelo início de cada trace; chegadas fora de ordem se reordenam naturalmente.
- Endpoint de listagem espelha o T10: filtros (período, agente, status), paginação; o de detalhe devolve a cadeia ordenada de traces + agregados.
*Habilita: US21–US23.*

### US21 — Lista de sessões
*Como gestor do cliente, quero navegar pelas conversas dos meus agentes (sessões) com seus totais — traces, duração, tokens, custo — para ver a atividade no nível da conversa, do jeito que meus clientes a vivem.*
- Colunas: ID da sessão, agente (+ caminho), nº de traces, status, duração total, tokens, custo (R$), início.
- Filtros: período, agente, status; paginação; estados vazios honestos.
- Semântica de período definida: a sessão entra no filtro pelo **horário de início** (traces que cruzam a borda do período ficam com a sessão — regra documentada).

### US22 — Drawer da sessão: a visão de conversa
*Como gestor do cliente, quero abrir uma sessão e ler a conversa — a cadeia cronológica de execuções com seu conteúdo — para ver exatamente como o agente tratou o meu cliente.*
- Linha de métricas agregadas (traces, duração, tokens, custo).
- Cadeia cronológica dos traces da sessão: cada entrada mostra hora, status, duração, custo e o **conteúdo (entrada/saída) legível em sequência** — a sessão se lê como uma transcrição.
- Cada trace da cadeia é clicável → drawer completo do trace (US19), e volta (a navegação preserva o contexto — o drawer sem pilha de "voltar" do mock é uma lacuna de UX conhecida, a corrigir no produto real).

### US23 — Custo de sessão consistente com o billing
*Como gestor do cliente, quero que o custo da sessão visivelmente some a partir dos seus traces, para que todo nível do produto conte a mesma história de dinheiro.*
- Custo da sessão = soma exata dos custos dos traces; arredondamento conforme a regra do T5; nenhum caminho de cálculo paralelo.

**Adiado:** sessões de voz (segunda linha de métricas: p95 voz→voz, interrupções, quedas, minutos) · anotações e compartilhamento de sessão · sinais de qualidade da conversa.

---

## Épico 5 — Billing: Fechamento do Mês

**Objetivo.** Com traces armazenados e já carimbados com preço (Épicos 1–2), o faturamento entra em cena: os meses fecham em snapshots completos de auditoria, reproduzíveis — a base da fatura. O custo já é imutável por trace desde a gravação; aqui ele é somado, congelado como extrato e vira compromisso.

### T6 — Ciclo de vida do mês e fechamento com auditoria completa
Máquina de estados por período: **aberto → fechado**. O fechamento gera um snapshot imutável que guarda **tudo que foi usado para calcular a fatura**.
- Períodos de mês-calendário; fechamento depois do fim do mês (gatilho automático vs. iniciado pelo admin = QA4; sem mecânica de carência na v1).
- O snapshot contém: os registros de uso em nível de trace, com as quantidades de tokens (a granularidade mais fina — os próprios traces); as versões exatas de preço aplicadas — que já estão **carimbadas trace a trace** (e são copiadas para o snapshot, não referenciadas); a saída do cálculo linha a linha e todos os agregados; a regra de arredondamento e a versão da lógica de cálculo; as exceções (sem preço / excluídos, com motivo e quem decidiu); timestamp do fechamento, gatilho e marca d'água da ingestão.
- **Teste de aceite de reprodutibilidade:** rodar o motor de novo sobre os insumos do snapshot reproduz as saídas ao centavo.
- O que o cliente vê é projeção do snapshot; o que guardamos é o pacote inteiro.
- Depois do fechamento: uso datado dentro do mês entra em quarentena como exceção visível ao admin (nunca é somado em silêncio); carimbo de pendentes daquele mês é bloqueado (só via reabertura); reagregação bloqueada.
- Fechamento bloqueado enquanto houver trace com 'preço pendente' no período.
- Reabertura existe no motor: auditada (quem/quando/por quê), gera nova **versão** de snapshot, as anteriores nunca somem — só via runbook na v1.
- **Regra canônica:** em mês fechado, o snapshot manda; traces em quarentena pós-fechamento são divulgados ('N execuções chegaram após o fechamento') — nunca somados em silêncio, nunca escondidos.
*Habilita: a validação da fatura (Épico 6) e a geração da fatura real.*

### US5 — Visibilidade do fechamento (admin)
*Como admin da plataforma, quero saber quando o mês fechou e ver o resumo do extrato final (ou o motivo do bloqueio), para gerar a fatura real com números que eu sei que são definitivos.*
- Notificação do resultado (fechado com total R$ X / bloqueado por pendências); extrato congelado recuperável na íntegra — ele é A base da fatura; mês reaberto mostra todas as versões com a nota de auditoria.

### US6 — Gestor vê o status do período com honestidade
*Como gestor do cliente, quero que toda tela deixe claro se estou olhando um mês fechado (final) ou em andamento (parcial), para nunca tratar número provisório como fatura.*
- Mês fechado rotulado como final; mês corrente marcado sem margem de dúvida como "em andamento — dados parciais" (com o timestamp da US2). Número de mês fechado bate com o snapshot exatamente, para sempre.

**Adiado:** tela de reabertura/ajustes com aprovação · créditos e ajustes manuais como linhas de extrato.

---

## Épico 6 — Aba Billing: Validação da Fatura

**Objetivo.** O gestor abre um mês fechado e confere — do total geral até qualquer linha — que a fatura é exatamente tokens × preço contratado. Prioridade nº 1 do produto.

### T7 — Camada de projeção do extrato
Camada de leitura que serve a aba Billing a partir dos snapshots (meses fechados) e dos agregados ao vivo (mês corrente).
- Mês fechado é servido **exclusivamente** do snapshot — nunca recalculado ao vivo.
- Os campos internos (US$, PTAX, markup, detalhe de exceções) não existem no schema de projeção — ausentes por construção, não escondidos pela UI.
- Responde com o status do período e a marca d'água de atualização (alimenta US2/US6).
- **Check automático de consistência no mês aberto:** agregados do billing ≡ soma dos custos carimbados dos traces (mesmo banco, mesmo carimbo — desvio é defeito).
*Habilita: US4, US6–US8, US10 e os épicos 7–8.*

### US7 — Visão do extrato mensal
*Como gestor do cliente, quero abrir um mês fechado e ver meu extrato: total em R$ e a quebra por agente, para que o primeiro número que eu conferir já bata com o total da fatura.*
- Seletor de mês (fechado = final; corrente = em andamento). Total no cabeçalho; tabela por agente com % do total; valores batendo com o snapshot ao centavo.

### US8 — Drill-down até a conta
*Como gestor do cliente, quero descer do total para qualquer agente → modelo → tipo de token, vendo quantidades, preços unitários e custo de cada linha, para eu mesmo refazer a conta de qualquer linha.*
- Cada nível mostra quantidade, preço contratado (R$/milhão) e custo — a visão "mostra a conta". Linha: agente × modelo × tipo de token → quantidade × preço = custo; os níveis-pai são somas exatas dos filhos (arredondamento documentado). Tipos de token: input, output, cache leitura, cache escrita. Os preços exibidos vêm do snapshot (conversa com a US4).

### US10 — Checagem de sanidade mês a mês
*Como gestor do cliente, quero o mês fechado lado a lado com o anterior (total e por agente), para enxergar anomalias na hora, antes de questionar a fatura.*
- Variação absoluta e %, total e por agente; só informativa na v1.

**Adiado:** fluxo de contestação · visão de drill diário (o motor guarda o diário).

---

## Épico 7 — Aba Billing: Tendências, Projeção e Composição (enxuta)

**Objetivo.** Sair de "a fatura está certa?" para "o gasto está sob controle, e o que vem por aí?"

### T8 — Séries temporais e cálculo de projeção
- Série mensal (total, por agente, por modelo) montada dos snapshots + mês corrente ao vivo — um total por mês, em todo lugar.
- Projeção: método definido e documentado — v1: run-rate linear (acumulado do mês ÷ dias completos decorridos × dias do mês). É estimativa derivada: nunca entra em snapshot, nunca aparece em mês fechado.
- Profundidade do histórico no lançamento limitada pelo backfill de ~49 dias: as tendências nascem rasas e engordam com o tempo.
*Habilita: US11–US12 (US13 adiada — ver Adiados).*

### US11 — Evolução do custo no tempo
*Como gestor do cliente, quero a evolução mensal do meu custo (total e por agente) ao longo do histórico disponível, para entender minha trajetória em vez de julgar cada mês isolado.*
- Gráfico + tabela; séries por agente ligáveis/desligáveis; mês em andamento visualmente distinto; valores fechando com o extrato de cada mês.

### US12 — Projeção do mês corrente
*Como gestor do cliente, quero um total projetado de fim de mês com base no consumo até agora, para antecipar a fatura e agir antes do fechamento, não depois.*
- Gasto até agora, total projetado e a base da conta em palavras simples; rotulada como estimativa sem margem de confusão; some quando o mês fecha; com menos de ~3 dias completos, "dados insuficientes para projetar".

---

### T9 — Métricas derivadas de composição
- **Economia de cache:** contrafactual "quanto custaria se as leituras de cache fossem cobradas como input normal" vs. o real, aos preços contratados (tratamento da escrita de cache explícito — QA7).
- **Mix de modelos:** participação de custo e de tokens por modelo, por agente e no total.
- **Participação dos segmentos ao longo dos meses** (motor pronto; a visão US9 foi adiada).
- Tudo derivado dos agregados de snapshot/ao vivo — sempre fechando com o total do mês.
*Habilita: US15 (US14/US16 adiadas — motor pronto; ver Adiados).*

### US15 — Visão de mix de modelos
*Como gestor do cliente, quero ver como o custo se divide entre modelos, por agente, para discutir com meu time se modelo caro está sendo usado onde um mais barato resolveria.*
- Participação de custo e tokens por modelo, com deslocamento mês a mês; "R$ médio por milhão de tokens" (taxa blended) por agente, para o mix ser interpretável.

---

## Épico 8 — Aba Billing: Exportação

### US17 — Exportar o extrato mensal
*Como gestor do cliente, quero baixar o extrato do mês fechado (resumo + quebras) como arquivo, para anexar a processos e relatórios internos.*
- PDF (apresentação) e CSV/XLSX (dados — as linhas da US8); renderizado do snapshot; carrega mês, status "final" e timestamp de geração. Mês em andamento: com marca d'água "parcial" ou bloqueado (decisão de design — QA13).

**Adiado:** envio agendado por e-mail · links de compartilhamento.

---

## Adiados — consolidado (pós-v1)

| Área | Item | Prontidão do motor na v1 |
|---|---|---|
| Billing — Validação | US9 — Custo por segmento de interação | Spans no contrato (T1) + série de segmentos no T9 — falta só a visão |
| Billing — Tendências | US13 — Destaques de variação | T8 tem as séries — falta só a visão |
| Billing — Composição | US14 — Composição visual · US16 — Economia de cache | T9 calcula (inclusive o contrafactual) — faltam só as visões |
| Reconciliação | US25 — Explicador 'uma história de dinheiro' | Essencial dobrado: regra canônica no T6, check no T7, deeplink na US7 |
| Ingestão | Fluxo de revisão de agentes novos | T3 já ingere agente desconhecido desde o dia 1 — falta só a tela |
| Ingestão | Tela de gestão de uso não classificado | T3 armazena e marca; atribuição mutável — falta só a tela |
| Ingestão | Política de retenção/expurgo e mascaramento (LGPD) | Postura explícita da v1: guarda tudo, sem máscara — decisão registrada |
| Ingestão | Canal de voz | T1 já carrega `channel`; voz entra como canal + tipos de span novos, sem migração |
| Preços | Tela de cadastro/alteração · visão de histórico | T4: schema versionado, constraints, runbook — falta só a tela |
| Preços | Análise interna de margem | T4 captura as colunas internas por versão desde o dia 1 — faltam só as visões *[**Superado pela decisão 96**: colunas internas removidas; margem/preço computado entram como um `pricingType` futuro que declara seus próprios campos]* |
| Preços | Construções contratuais (mínimos, faixas, taxas) | Motor novo quando priorizado |
| Motor | Tela de reabertura/ajustes + aprovações · créditos como linha | T6: reabertura auditada e versionada — runbook na v1 |
| Billing | Fluxo de contestação · drill diário | Snapshot guarda o nível de trace; diário armazenado |
| Tendências | Alertas de orçamento · projeções melhores · anomalias | Projeção do T8 e série diária existem |
| Composição | Recomendações · custo por conversa | As sessões já dão a contagem de conversas — candidata a antecipação |
| Exportação | Envio por e-mail · links | A renderização da US17 é a base |
| Traces | Filtros salvos · exportar lista · custo por span | O armazenamento tem os dados |
| Sessions | Métricas de voz · anotações · sinais de qualidade | Read-model extensível |
| Transversal | RBAC (papéis e hierarquia) · nomes de nível configuráveis | v1 é single-tenant; domínio/subdomínio já vão no contrato como strings |
| Transversal | Custos de infra/compute | O motor não deve chumbar "custo = tokens" |
| Transversal | Operação multi-tenant | Superada: o modelo é uma instância por cliente; revisitar só se operar N instâncias doer |

---

## Log de decisões

| # | Decisão | Implicação |
|---|---|---|
| 1 | Persona principal: gestor do lado do cliente | Tudo que o cliente vê é em R$ e em linguagem simples |
| 2 | Os quatro objetivos do gestor no escopo; validar fatura em 1º | Ordem: transparência → tendências → composição → exportação |
| 3 | O relatório é a base de uma fatura real | Reproduzir a fatura com exatidão = o coração da confiança |
| 4 | Preço contratado é dado armazenado, não cálculo | Tem markup; custo de mercado varia; T4 com versões por vigência |
| 5 | Contratos em R$; câmbio/PTAX é assunto interno | US$/PTAX/markup nunca chegam ao cliente; excluídos no schema (T7) |
| 6 | Período de faturamento = mês-calendário | Ciclo aberto → fechado; mês corrente sempre parcial |
| 7 | LangWatch é o conector entre agentes e plataforma | Uma fonte de ingestão para as três abas |
| 8 | Infra/compute fora da v1 | Só tokens; motor extensível a novas categorias |
| 9 | Controle de acesso e papéis adiados | v1: o cliente loga e vê os dados da sua instância |
| 10 | História adiada mantém as tech stories que a habilitam | Adia-se a tela, não a capacidade |
| 11 | Preço mantido direto no banco na v1 | A disciplina vai para o schema: versões imutáveis, constraints, runbook |
| 12 | Sem mecânica de carência | Trace atrasado = exceção em quarentena, resolvida por gente |
| 13 | O snapshot guarda TUDO que calculou a fatura | Auditoria completa e autocontida; reprodutibilidade é teste de aceite |
| 14 | Nome cru de ferramenta serve na v1 | Sem dicionário de rótulos amigáveis |
| 15 | Uma API, três partes/abas: Billing, Traces, Sessions | Um contrato de metadados com matriz campo × consumidor |
| 16 | Trabalho principal de Traces/Sessions: transparência e confiança | Custo por trace em primeiro plano; deeplink billing→traces é essencial |
| 17 | Mesmo modelo de acesso do billing; RBAC adiado | Escopo garantido no nível do motor |
| 18 | Conteúdo das conversas na íntegra, sem mascaramento | O contrato carrega os payloads; retenção fica como ponto aberto registrado |
| 19 | **Single-tenant: uma instância por cliente** | Isolamento vira isolamento de deployment; o nível "organização" da hierarquia cai |
| 20 | Domínio e subdomínio são strings simples | Tags informativas + filtros simples; sem cascata nem nomes configuráveis |
| 21 | Guardar tudo (traces, spans, payloads) — o LangWatch retém só ~49 dias | A plataforma é o arquivo permanente; o sync é crítico no tempo; o backfill fica limitado a ~49 dias (o que passou disso já era) |
| 22 | Agregados do billing derivam dos traces armazenados; uma precificação só | Um banco, um carimbo por trace — consistência entre abas por construção |
| 23 | Só agentes de texto na v1 | Voz adiada; o campo `channel` deixa a porta aberta |
| 24 | Ordem de construção reorganizada: Traces → Sessions → Billing | Segue a cadeia de derivação (trace é o átomo); a validação da fatura passa a chegar por último — troca consciente; IDs de histórias preservados |
| 25 | **Preço é carimbado no trace no ato da ingestão; carimbo imutável** | Preços (T4) viram o Épico 1 — pré-requisito do pipeline; mudança de preço só afeta traces futuros; correção de carimbo só via reabertura auditada; imutabilidade migra do fechamento para a escrita |
| 26 | Cortes v2.3: US9, US13, US14, US16 adiadas | Visões de análise; o motor que as alimenta (T8/T9) permanece na v1 |
| 27 | Reconciliação dobrada no essencial (v2.3) | Épico dedicado cortado; regra canônica → T6, check de consistência → T7, deeplink extrato→traces → US7; US25 adiada |
| 28 | (PoC) Correções de fundação no boilerplate antes do pipeline | adaptRoute com try/catch (erro vira 500 padronizado, processo não cai); registro de rotas v1 com glob ancorado e aguardado (`routesReady`); `MongoDb.disconnect` limpa o client e ganha `connectWithUri`; testes de banco usam o Mongo em memória do preset jest-mongodb via `MONGO_URL` (sem mongod local; config `.mjs` morta removida); tsconfig `module=node16` (build/typecheck voltam a funcionar) com ts-jest emitindo CJS só nos testes |
| 29 | (PoC) Dinheiro em inteiros de micro-centavos (µ¢, 1e-8 R$) | Preço = inteiro µ¢/milhão de tokens (fino o bastante para preços derivados de US$×PTAX×markup); custo carimbado = inteiro µ¢ (half-up no µ¢, produto intermediário em BigInt); somas exatas até 2⁵³ µ¢ ≈ R$ 90 mi (limite assertado); exibição half-up 2 casas com ajuste largest-remainder para as partes fecharem com o total (T5); nunca float em aritmética ou armazenamento |
| 30 | (PoC) Migrations/seed em runner mínimo próprio (Mongo não tem SQL) | `runMigrations(db, migrations)` exportado (jobs E testes de integração o executam), coleção `migrations` marca aplicadas; constraints do T4 viram unique indexes; troca de preço no meio do período e modelo sem preço já entram no seed; runbook de preço = script `npm run price:insert` (insert-only) |
| 31 | (PoC) Store em 3 coleções: `traces`, `spans`, `trace_contents` | Chave natural = `traceId` do LangWatch (idempotência do sync); escrita parcial-segura: spans/conteúdo primeiro, documento do trace por último como marcador de commit, sobras de execução interrompida são limpas no re-run |
| 32 | (PoC) Estado de precificação é campo próprio (`pricingStatus`), ortogonal ao `status` | `status` segue ok/erro (contrato T1, filtro T10); `pending_price` não polui o status de execução; pendente = qualquer tipo de token USADO sem preço vigente — nunca carimbo parcial, nunca custo 0 |
| 33 | (PoC) Re-sync de trace já ingerido atualiza SÓ atribuição | Operação `updateAttribution` estruturalmente sem campos de carimbo (invariante 7); janelas de sync são half-open `[from, to)`; detecção de buracos e alerta de retenção ficam como stub de log (T2 completo é pós-PoC) |
| 34 | (PoC) Sessão calculada no read (pipeline GROUP BY sessionId), não materializada | Agregados fecham por construção; atribuição da sessão = a do primeiro trace; período filtra pelo início da sessão (QA17); `lastActivity` = maior `finishedAt` |
| 35 | (PoC) Honestidade de custo com pendências em TODA projeção | Trace pendente: `cost_brl: null` (+`pricing_status`); sessão com pendência: `cost_brl: null` + `stamped_cost_brl_partial` + `pending_price_count` — nunca um total que se leia como R$ 0,00 final |
| 36 | (PoC) Exibição: linha em precisão cheia, total com 2 casas | Detalhe do trace mostra preço aplicado e custo por tipo de token em string BRL exata (`cost_brl_exact`); só totais exibidos arredondam (half-up); datas e filtros de período em UTC, janelas `[from, to)` |
| 37 | (PoC, pós-revisão) Rotas v1 registradas estaticamente | O auto-registro por fast-glob só funcionava sob tsx/jest (glob de `.ts` relativo ao cwd): o build compilado (`node dist`) quebrava no boot — confirmado empiricamente. Rota nova = 1 import + 1 linha em `v1-routes-setup.ts`; verificado com o servidor `dist` servindo os endpoints |
| 38 | (PoC, pós-revisão) `model` é atribuição mutável; flag `unclassified` derivada do documento armazenado | `updateAttribution` aceita `model` (correção re-agrupa billing e destrava reprocess de pendentes — nunca toca o carimbo); o flag é recomputado pelo repositório APÓS o merge (payload sem um campo não re-flagra correção feita no store, nem limpa flag com valor ainda ausente); traces pendentes persistem `pendingPrice.missingTokenTypes` (US3), limpo no carimbo; seed de preços idempotente (upsert) para o runner sobreviver a crash entre aplicar e registrar |
| 39 | (PoC) Vertical de exemplo do boilerplate removido | Sign-up/Account inteiro (core→rotas, com bcrypt/email-validator e specs), config JWT (schema + `.env*`), helper `safeParse` e deps órfãs (`neverthrow`, `uuid`, `bcrypt`, `validator`, `ts-node`) removidos — nada era consumido pelo PoC; os jobs deixam de exigir `JWT_SECRET`; suíte 115 testes verdes, build `dist` servindo e `POST /signup` respondendo 404 |
| 40 | Cliente LangWatch REAL implementado atrás da mesma interface (QA14 fechada) | `HttpLangWatchClient` (search paginado + detalhe N+1, schema zod tolerante, mapper → contrato T1); swap por configuração: `LANGWATCH_ENDPOINT`/`LANGWATCH_API_KEY` presentes → real, ausentes → fake (testes/demo offline); credenciais só em `.env*` (gitignored). Mapeamentos: sessão=thread_id, agent=metadata.agent∥service.name, cache tokens da metadata reservada (strings) com fallback para Σ spans, finished_at derivado de total_time_ms, trace multi-modelo → `model` indefinido (pendente/não classificado — nunca precificado pelo modelo errado; precificação por span fica como decisão pós-PoC). Validado ao vivo: 25 traces reais ingeridos, preços registrados via runbook, reprocess carimbou 12 (9 multi-modelo seguem pendentes com motivo), billing 2026-07 parcial fechando com Σ display |
| 41 | Fornecedor de traces é 100% trocável — port neutro + teste de arquitetura | Port renomeado para `TraceSourceClient`/`SourceTrace` (`data/interfaces/trace-source-client.ts`, shape = contrato T1); adapter do vendor confinado a `infrastructure/traceSource/langwatch/` e o fake (contrato) em `infrastructure/traceSource/`; composition root escolhe o vendor. `architecture-boundaries.spec.ts` FALHA o build se: o nome do vendor aparecer em core/data/presentation/common, ou se qualquer camada importar na direção errada (core→nada externo, data→só core, presentation→só core). Trocar de vendor = novo adapter + 1 linha no factory + env próprio — zero mudança em regra de negócio (o teste garante) |
| 42 | Agente e canal viram blocos denormalizados com versão e instância (omni e agentes escalam horizontalmente) | Contrato T1 e store: `agent {id, version?, instance?}` (substitui agentId) e `channel {type, version?, instance?}` — versão (build) e instância (réplica) são fatos do MOMENTO da execução, congelados no trace como o carimbo (nunca resolvidos de um registro mutável). Identidade continua `agent.id`/`channel.type` (filtros, sessões, billing agrupa por `agent.id`; quebra por versão é análise futura sobre dados já capturados); versão/instância ausentes não desclassificam. Convenção de metadata com fallback OTel semconv: `agent`∥`service.name`, `agent.version`∥`service.version`, `agent.instance`∥`service.instance.id`; `channel`, `channel.version`, `channel.instance` (deployment do omni). Migração 004 remodela traces existentes (só atribuição — carimbo intocado) e repõe índices (`agent.id`, `channel.type`); novo filtro `channel` no GET /traces |
| 43 | Convenção de armazenamento: campo opcional grava como `null`, nunca ausente | Todo documento mostra o schema completo no banco (pedido do usuário para inspeção). Domínio segue com `undefined`/`?`; o `null` é representação de storage aplicada na fronteira de escrita (mapper nomeia todas as chaves — blocos agent/channel e os 4 tipos de token sempre presentes — e o BSON serializa undefined→null); `unclassified`/`pendingPrice` limpos viram `null` (não `$unset`); trace pendente mostra `totalCostMicrocents: null` = custo EM ABERTO (nunca 0 — invariante 2 preservada); migração 005 uniformiza documentos legados (traces, spans, price_versions) sem tocar carimbos |
| 44 | OpenAPI/Swagger gerado dos schemas de projeção (fonte única de verdade) | Schemas zod ESTRITOS de resposta vivem na presentation (por feature, ao lado dos view-models, que são TIPADOS por eles — drift não compila); o documento OpenAPI 3.1 é gerado deles via `z.toJSONSchema` nativo e montado no composition root (`main/docs/openapi.ts`); UI + JSON em `/api/v1/docs` (registrado antes do middleware de content-type JSON). Testes de contrato parseiam as respostas REAIS com os schemas estritos (docs nunca mentem; campo vazado quebra a suíte) e a invariante 4 é checada também sobre o próprio spec |
| 45 | Spans embutidos no documento de conteúdo — collection própria eliminada | Decisão de produto: spans são consumidos SOMENTE na visualização do trace. Eles moram em `trace_contents.spans` (o payload detail-only), não na coleção quente `traces` (métricas+carimbo continuam mínimos para listas/sessões/billing) nem em coleção própria (3 coleções → 2; uma leitura a menos no detalhe; API pública inalterada — provado pelos testes de contrato). `SpanModel` perde o FK `traceId`; sessões projetam `spans` para fora do fetch de conteúdo; migração 006 embute os legados em ordem cronológica e derruba a coleção `spans` (constraint: reavaliar se custo por span ou busca de spans entre traces entrarem no produto) |
| 46 | `spanContents` fundido nos spans embutidos | O array paralelo era cicatriz do layout antigo (metadata e payload de span em lugares distintos); com tudo no mesmo documento, cada span carrega seu próprio `input`/`output` (null quando a fonte não instrumentou). Contrato da API acompanhou: span do detalhe expõe payload próprio, `content.span_contents` removido (schemas + testes de contrato atualizados); a projeção `{spans: 0}` do detalhe de sessão agora corta também os payloads de span (antes vazavam no fetch da cadeia). Migração 007 funde os legados; carimbos intocados |
| 47 | `traces` e `trace_contents` fundidos: um trace = um documento autocontido | Pedido do usuário, trade-off aceito conscientemente: agregações (sessões GROUP BY, billing $unwind) passam a carregar documentos completos do disco — REVISITAR no dimensionamento do QA15; risco do teto de 16MB por documento passa a existir para conversas gigantes. Ganhos: escrita do sync ATÔMICA (marcador de commit e limpeza de sobras eliminados por construção), detalhe em UMA leitura, modelo mínimo (2 coleções → 1 de traces). Mitigações: listas e cadeia de sessão projetam spans/payloads para fora; billing projeta cedo no pipeline. API pública inalterada (testes de contrato provam). Migração 008 funde e derruba `trace_contents` |
| 48 | Endpoints atualizados para o modelo final; billing quebra por VERSÃO do agente | Lista de traces e resumo de sessões expõem os blocos completos `agent {id, version, instance, domain, subdomain}` e `channel {type, version, instance}` (rollout visível sem abrir o detalhe); sem filtros novos (decisão do usuário); linhas do billing viram agente × **versão** × modelo × tipo de token (revisa a parte "billing agrega só por id" da decisão 42 — custo por release visível no extrato; total inalterado, consistência ≡ Σ carimbos mantida com a chave nova no teste). Sessões mantêm `last_activity_at` (poc.md/T11) e os campos de pendência (invariante 2) além das colunas da US21. Ajuste posterior: `domain`/`subdomain` saíram do bloco `agent` para a RAIZ dos itens de trace e sessão — são atributos do trace (decisão 20), não do agente |
| 49 | Billing summary recebe `year` e `month` como parâmetros separados (ambos obrigatórios) | Pedido do usuário: `GET /billing/summary?year=2026&month=6` substitui `?month=YYYY-MM` (desvio consciente do literal do poc.md); a resposta espelha a separação (`year`/`month` inteiros). Validação: ambos obrigatórios (400 MissingParam), ano 1970-9999, mês 1-12; semântica de mês-calendário UTC inalterada |
| 50 | Fatura como recurso de primeira classe: `GET /bills` lista as faturas (uma por mês-calendário UTC com ao menos um trace) | Pedido do usuário durante a UI da PoC: a tela de billing lista FATURAS, não meses — nomenclatura corrigida de ponta a ponta (`ListBillsUseCase`, `listBills()`, resposta `{bills: []}`). Cada item traz status do período (mês corrente sempre `in_progress`/parcial — invariante 8), contagens de traces carimbados e pendentes (pendentes fora do total — invariante 2), tokens e total ≡ Σ carimbos do mês (invariante 3; teste de integração pina a linha da fatura ao extrato). O extrato do mês continua em `GET /billing/summary?year&month` e é o detalhe aberto ao clicar na fatura |
| 51 | API entrega TODO valor derivado/exibido; front-end só renderiza | Pedido do usuário: nenhum processamento de dados no front. Três camadas: (a) traces são snapshots imutáveis → campos derivados CONSOLIDADOS no documento na ingestão (`tokensTotal`, `span.offsetMs`; migração 009 backfilla); (b) sessões e billing são read-models derivados → agregados calculados em tempo de consulta (agrupamento por agente do extrato, geometria das barras, contagens) — nunca armazenados; (c) formatação de exibição (pt-BR, R$, durações, datas, idade relativa, rótulos, geometria do waterfall, `total_pages`) vive nos view-models da presentation — campos `*_display`/`*_label` no contrato. Datas exibidas no fuso fixo UTC-3 (America/Sao_Paulo, sem DST desde 2019); formatação manual determinística, sem dependência de ICU. Displays de grupo do billing somam os centavos RECONCILIADOS das linhas — grupos fecham com o total como as linhas (T5) |
| 52 | Topologia de deploy dockerizada: 1 projeto compose POR CLIENTE + infra compartilhada mínima | Isolamento single-tenant no nível de deployment (invariante 5): cada cliente (hapvida/claro/vivo) é um projeto compose próprio (`compose.client.yml` + `clients/<nome>.env`) com API própria e instância LangWatch self-hosted própria (app pinado em `langwatch/langwatch:3.5.0` + postgres + redis + clickhouse — stack oficial atual, SEM OpenSearch; contêiner de workers separado como no upstream — `START_WORKERS=true` não funciona na imagem de produção). Compartilhados: um MongoDB (um database por cliente, `MONGO_DB_NAME`) e o mock do endpoint de discovery (`GET /resolve/:cliente` → `apiBaseUrl`), que a UI consulta ao trocar de cliente no seletor — a UI não conhece endereço de API. Segredos LangWatch por instância em `clients/*.env`; `LANGWATCH_API_KEY` vazio → sync usa o cliente fake de fixtures (QA14); preenchido após onboarding → cliente HTTP real via DNS interno do projeto *[**Emendada pelas decisões 139/140 (10/08/2026)**: `MONGO_DB_NAME` → `MONGO_USAGE_DB_NAME` obrigatório; em produção a topologia vira fábrica AWS (Fargate + EC2 LangWatch + cluster Atlas por cliente) — o projeto compose por cliente permanece para dev/demos]* |
| 53 | Massa de demonstração gerada por cliente, com custo por trace entre R$ 1 e R$ 100 | Pedido do usuário: dados realistas e variados por cliente. `packages/api/scripts/generate-demo-fixtures.mjs` gera fixtures determinísticas por cliente (perfis distintos de volume, modelo, agente, canal, sessão, duração, spans, tokens e status) em `demo-data/<cliente>/`, montadas no contêiner sobre o diretório de fixtures da imagem — a ingestão continua sendo a normal (carimbo no ato da gravação; o gerador NUNCA escreve no banco). Como o custo vem da tabela de preços, o gerador espelha o lookup as-of e dimensiona os tokens para acertar o custo alvo; para manter contagens de tokens plausíveis em toda a faixa foi registrado um modelo premium (`anthropic/claude-opus-4-8`, R$ 82,50/M input e R$ 412,50/M output) via o job de preços (invariante 9), e não por migração. Tráfego de 01/06 a 21/07/2026: junho fecha completo, julho é parcial. Sem traces `pending_price` nesta massa — o requisito era que todo trace tivesse custo |
| 54 | Dockerização em forma de produção: unidade de deploy 100% autocontida por cliente | Refina a decisão 52 rumo à produção: `compose.client.yml` passa a ser a ÚNICA unidade de deploy — api + mongo PRÓPRIO (volume próprio, auth opcional via env, sem porta no host) + stack LangWatch completa, sem rede compartilhada e sem infra pré-existente; N clientes = N aplicações do mesmo arquivo com N env files (`clients/example.env` é o contrato; arquivos reais fora do git). Nada — imagem, compose, aplicação, Makefile — conhece cliente algum: o Makefile vira genérico (`make up CLIENT=<nome>`), o bloco `build` e as fixtures de demo migram para `compose.dev.yml` (só dev), e o mock de discovery fica em `compose.shared.yml` como ferramenta de dev fora do deploy. Única mudança de código: a URI local do Mongo passa a aceitar credenciais opcionais (`authSource=admin`, retrocompatível). Dados dos 3 clientes demo re-sincronizados do LangWatch de cada um (fonte da verdade upstream; mongo compartilhado aposentado) |
| 55 | Endpoint de discovery era artefato de teste — descartado, não é feature futura | Corrige a premissa registrada nas decisões 52/54: o `GET /resolve/:cliente` existiu apenas para a UI de teste multi-cliente do PoC. Com a UI dentro da stack de cada cliente (mesma origem, decisão 54), o mock perdeu o único consumidor e foi removido do repositório junto com seus alvos de tooling. Nenhum componente futuro depende desse contrato; se algum dia um roteamento multi-cliente existir, será especificado do zero |
| 56 | Fase 1 do desacoplamento de storage: a troca de banco fica confinada a infrastructure + factories + config | Auditoria profunda (49 agentes, 0 bloqueadores no domínio) seguida de refatoração: (a) porta `Database`/`MigrationRunner` — `main/factories/database-factory.ts` é o ÚNICO lugar que nomeia um backend concreto; entry points consomem a porta; (b) catálogo de migrações Mongo movido para `infrastructure/database/mongodb/migrations` (cada backend é dono do seu catálogo); (c) violação de unicidade cruza a fronteira como `DuplicatePriceVersionError` tipado — nunca mais sniffing de texto de erro do driver; (d) regra de `unclassified` tem UMA definição (`deriveUnclassified`) compartilhada entre mapper e adapter; (e) provas dos invariantes viram SUITES DE CONTRATO das portas (`data/interfaces/*.contract.ts`), parametrizadas por harness — qualquer adapter futuro roda as mesmas provas; testes de rota ficam cegos de storage atrás de um único helper; (f) suite unitária não sobe mais mongod; (g) fitness tests novos impedem regressão: driver `mongodb` só dentro do adapter, camadas de negócio cegas de storage (testes incluídos), main sem imports profundos de storage fora do composition root. Fase 2 (adapter Postgres) fica trivial quando/se necessária |
| 57 | Totais de tokens da plataforma incluem os QUATRO tipos cobrados — divergência com o card "Tokens" do LangWatch é esperada | O headline do dashboard do LangWatch soma apenas prompt+completion; a plataforma soma input+output+cache_read+cache_write, porque tokens de cache têm preço contratado em R$ e o número de tokens exibido precisa fechar com o R$ ao lado (invariante 3 é computado sobre os quatro tipos). Ex. real (cliente vitoria): LangWatch 18,7M = input+output 18.704.574; plataforma 21.939.800 = + cache_read 2.619.690 + cache_write 615.536. Não é bug de sync — o extrato da fatura mostra a decomposição por tipo. Registrado no README (seção LangWatch) para não virar chamado de suporte |
| 58 | Camadas renomeadas: `src/core` → `src/domain` e `src/data` → `src/application` | Pedido do usuário: os nomes do boilerplate (estilo Manguinho) confundiam — dois diretórios `useCases` sem indicar que um guarda contratos e o outro implementações. Rename puro via `git mv` + reescrita de imports relativos; nenhuma mudança de semântica: `domain/useCases` = interfaces + modelos (zero dependências), `application/useCases` = implementações `Db*` sobre as portas de `application/interfaces`. `architecture-boundaries.spec.ts` atualizado para os novos nomes (direção de dependência e cegueira de storage/vendor seguem enforçadas por teste). Referências históricas a `core/`/`data/` em decisões anteriores (41, 56) permanecem como registro da época |
| 59 | Ingestão contínua lê DIRETO do ClickHouse do LangWatch (mesma rede do compose), não da API HTTP | Resolve QA1 (frescor) e o teto da busca (~100 traces/janela + N+1 de detalhe por trace, inviável no volume esperado). Terceiro adapter do port `TraceSourceClient` (`infrastructure/traceSource/langwatch/clickhouse/`): `trace_summaries` + `stored_spans` em SQL batelado; fidelidade verificada por diff campo-a-campo de um trace ingerido pelos DOIS caminhos (0 divergências). Acoplamento ao schema interno: (a) pinado pela tag da imagem no compose; (b) tripwire em runtime — o worker checa a versão de migração (`goose_db_version` = 35) no startup e se recusa a rodar (crash-loop visível) se o schema mudou; upgrade do LangWatch = revalidar SELECTs/mapper + atualizar a constante. Cadeia do factory: ClickHouse → HTTP → fake; `make sync` manual também usa o ClickHouse quando configurado (sem teto). O cliente HTTP fica como fallback/spot-check |
| 60 | Sync recorrente = sidecar `sync-worker` com loop de watermark (cursor persistido), não cron | Mesmo padrão do `langwatch-workers` (mesma imagem, outro command); viaja com o deploy de cada cliente, `restart: unless-stopped`. Loop: lê cursor → batches ≤ `SYNC_BATCH_SIZE` (memória limitada por construção, qualquer backlog) → ingere TUDO → só então avança o cursor (`sync_state` no Mongo). Cursor = `(UpdatedAt, TraceId)` do ClickHouse — tempo de ESCRITA na fonte, não do trace: chegada atrasada fica sempre à frente do cursor (estruturalmente impossível de perder). Crash/SIGKILL no meio do batch: cursor não avançou, re-leitura é deduplicada por `insertIfAbsent`. SIGTERM: termina o batch corrente e sai limpo (`stop_grace_period: 60s`). Loop não se sobrepõe por construção (um ciclo termina antes do próximo). Erros transitórios: retry com backoff dobrado; tripwire de schema: fatal |
| 61 | Quarentena de 15 min antes de ingerir (`SYNC_QUIET_PERIOD_SECONDS=900`) | O LangWatch monta traces incrementalmente (spans pingam) e o carimbo é imutável — sincronizar cedo demais congelaria contagens parciais de tokens. Só entram linhas sem atualização há 15 min; frescor do billing/traces fica ~15-16 min atrás do vivo. Escolha do usuário (23/07/2026) entre 1/5/15 min |
| 62 | Linha venenosa: skip + log, nunca travar o sync | Linha que falha validação/mapeamento é pulada com o `traceId` no log e o cursor avança por cima dela (senão um registro malformado pararia a ingestão para sempre). Escolha do usuário (23/07/2026) contra a alternativa halt-loudly; o log é a trilha de recuperação |
| 63 | Reprocess com gatilho duplo | (a) Imediato: `price:insert` roda o reprocess ao final (preço novo re-carimba na hora o que destravou); (b) backstop: o worker varre a cada `REPROCESS_INTERVAL_SECONDS` (default 1h). Decisão do usuário (23/07/2026): "both" |
| 64 | Retenção do LangWatch é configurável — subir de 49 dias no onboarding | Descoberta (instância viva 3.5.0): 49 = `PLATFORM_DEFAULT_RETENTION_DAYS` carimbado POR LINHA no ClickHouse quando a tabela `RetentionPolicy` (Postgres) está vazia; overrides por org/team/project (49 dias a ~179 anos, semanas inteiras) via `/settings/data-retention`, com re-carimbo retroativo. Novo passo de onboarding recomendado: override org-level de `traces` (ex.: 1 ano) — relaxa o prazo de perda de dados sem tocar na invariante 6 (nossa store segue sendo o arquivo permanente). Decisão 21 segue valendo como default |
| 65 | Compose fatiado por PAPEL: `compose.module.yml` (api+ui+sync-worker) + `compose.langwatch.yml` (conector) + `compose.mongodb.yml` (banco) | Substitui o `compose.client.yml` monolítico; os três arquivos se fundem no MESMO projeto por cliente (rede/DNS/volumes idênticos — config renderizada byte-idêntica, verificada por diff; zero recriação de contêineres). Regra estrutural: cada acoplamento mora no arquivo que o introduz — o arquivo do conector carrega os fragments de env/depends_on que ele impõe ao módulo (LANGWATCH_*, clickhouse healthy), o do banco idem (mongo healthy); o módulo não nomeia serviço de conector nem de banco. `MONGO_DB_HOST/PORT` viram env-driven (default = mongo in-project). Futuro já comportado: mais conectores = novos `compose.<conector>.yml` irmãos; banco externo (ex.: Atlas) = tirar `compose.mongodb.yml` da cadeia + apontar env — sem editar arquivo nenhum. Makefile centraliza a cadeia em `COMPOSE_FILES` |
| 66 | `sync-worker` movido do módulo para o arquivo do conector (`compose.langwatch.yml`) | Pedido do usuário (27/07/2026): a granularidade de escala horizontal do ingestor é a do LangWatch — o worker existe para drenar o ClickHouse DESTE conector, então outro stack LangWatch = outro ingestor, e o serviço mora no arquivo que define o conector (refina a decisão 65: módulo = api+ui). Consequências: (a) cadeia sem conector não tem worker nenhum (antes: worker ocioso) — `make sync` sobre fixtures segue como caminho offline; (b) o fragment `depends_on: mongo` do worker saiu de `compose.mongodb.yml` e não foi recriado — compose quebra tanto com fragment sobre serviço indefinido (cadeia módulo+banco) quanto com depends_on para serviço inexistente (conector+banco externo); o ordering de boot fica coberto pelo server selection de 30s do driver mongo + `restart: unless-stopped`. Config renderizada da cadeia completa ficou idêntica exceto a remoção desse depends_on (verificado por diff das 4 combinações de cadeia) |
| 67 | Renomeações: serviço `sync-worker` → `trace-ingestion-worker`; arquivo `compose.langwatch.yml` → `compose.connector.yml` | Pedido do usuário (27/07/2026), na sequência da decisão 66: o serviço ganha nome que diz o que faz (ingestão de traces), e o arquivo do conector passa a ser nomeado pelo PAPEL, não pelo vendor — trocar de conector no futuro = outro `compose.connector.yml` no mesmo slot da cadeia (refina a parte "sibling compose.<conector>.yml" da decisão 65). Identificadores internos do código (`SyncWorkerEnvironmentVariables`, `syncWorkerSettings`, env vars `SYNC_*`, `run-sync-loop.js`) ficam — descrevem a atividade de sync, não o contêiner. `make up`/`up-prod` ganham `--remove-orphans`: sem isso o rename deixaria o contêiner antigo rodando junto do novo — dois ingestores disputando o mesmo cursor de watermark |
| 68 | Rename estendido ao código (reverte a ressalva da decisão 67, a pedido do usuário) | Env vars `SYNC_INTERVAL_SECONDS`/`SYNC_BATCH_SIZE`/`SYNC_QUIET_PERIOD_SECONDS` → `TRACE_INGESTION_*` (contrato de env quebra: env files de clientes existentes atualizados junto — cliente1/cliente2 — e `1-init-client-env.sh` escreve os nomes novos); `SyncWorkerEnvironmentVariables` → `TraceIngestionWorkerEnvironmentVariables`; `syncWorkerSettings` → `traceIngestionWorkerSettings`; entry point `run-sync-loop.ts` → `run-trace-ingestion-loop.ts` (comando do compose atualizado — exige imagem rebuildada antes do próximo `up`); npm script `sync:loop` → `ingestion:loop`; prefixo de log "Sync worker:" → "Trace ingestion worker:". Ficam com nome de sync o que É sync (atividade): `sync-factory.ts`, use cases `Sync*`, `run-sync.js` (backfill manual), linhas de log "Sync: batch", `REPROCESS_INTERVAL_SECONDS`. Verificado: tsc limpo, 201/201 testes, config das cadeias renderiza os knobs novos, imagem rebuildada contém `run-trace-ingestion-loop.js` |
| 69 | Limpeza de código morto (27/07/2026): removidos `openapi.json`/`api-docs.html` da raiz, `populate-langwatch.sh`, ambiente `staging`, deps npm não usadas e o lockfile órfão de `packages/api` | Auditoria de não-uso: os artefatos de docs da raiz eram cópias estáticas do commit inicial — o documento vivo é gerado em memória (`main/docs/openapi.ts`) e servido em `/api/v1/docs`; `populate-langwatch.sh` foi superado pelo fluxo `4-seed-demo-data.sh` + `push-demo-to-langwatch.mjs`; nada em repo nenhum seta `ENVIRONMENT=staging` (enum + `.env.staging` removidos — `production`/`test`/`development` ficam); deps removidas de `packages/api`: `cors` + `@types/cors` (middleware CORS é feito à mão), `@types/mongodb` (driver v7 traz os próprios tipos), `@jest/globals` (nenhum import; segue como dep transitiva do jest), `git-commit-msg-linter` (nenhum hook instalado); `packages/api/package-lock.json` (lockfileVersion 1) era resquício do boilerplate — o lockfile da raiz governa o workspace. Verificado: tsc limpo, 201/201 testes, UI carrega sem erros de console |
| 70 | Enriquecimento display-only do trace (28/07/2026): `userId`, `environment` e o bloco `experiment` (A/B) passam a ser ingeridos, armazenados no documento e expostos nas projeções de traces e sessões — SEM novos filtros, SEM dimensão de billing, SEM impacto no unclassified | Alinhamento de contrato com o martino-agent (que já emite `langwatch.user.id`, `deployment.environment` e `ab.experiment`/`ab.variant`/`ab.variant_version`). Cadeias de fallback: usuário = `user_id` ∥ `langwatch.user.id` ∥ `user.id`; ambiente = `deployment.environment` ∥ `.name` (rename do semconv); experimento só quando `ab.experiment` E `ab.variant` presentes (nunca bloco pela metade). `userId` de sessão segue a regra PoC do primeiro trace (mesma do `agent`). Escopo decidido com o usuário: armazenar + exibir apenas |
| 71 | `customer_id`/tenant deliberadamente NÃO ingerido | Conflita com a invariante 5 (single-tenant — nenhuma chave de tenant no modelo de domínio); a emissão `langwatch.customer.id` do agente está dormante (nenhum caller passa o valor). Se multi-tenant um dia entrar, é decisão de produto nova — não entra de carona pelo contrato de metadata |
| 72 | Fallbacks de tokens OpenInference (`llm.token_count.prompt_details.cache_read`/`cache_write`) adicionados APENAS no adapter ClickHouse, como defesa — `gen_ai.usage.*` sempre vence quando presente | Evidência das linhas reais 3.5.0 (fixtures do spec) mostra que o LangWatch já normaliza as chaves OpenInference do stack agno para `gen_ai.usage.*` — o fallback cobre stacks cujas chaves não sejam normalizadas. O adapter HTTP não tem paridade POR CONSTRUÇÃO: a API expõe só `span.metrics` computadas, nunca attributes crus. Fallback de input/output (`llm.token_count.prompt`/`completion`) NÃO entrou: no OpenInference `prompt` INCLUI os tokens de cache — ligar ingênuo dupla-contaria; adiado até verificação viva da semântica |
| 73 | Traces históricos NÃO são re-ingeridos para os campos novos — carregam null (migração 011 faz backfill de nulls pela convenção full-schema) | Os campos novos são enriquecimento display-only, não atribuição: `updateAttribution` (invariante 7) segue cobrindo só agent/model/domain/subdomain. Re-ingestão em massa violaria a imutabilidade prática do documento por benefício nenhum — os campos só fluem para traces novos. **Superada pela 74**: a migração 011 foi removida junto com todo backfill (deployments partem do zero) |
| 74 | Poda das migrações (28/07/2026): a cadeia carrega SÓ bootstrap determinístico — 001 (índices de preço) + 003 (índices de trace). Removidas: 002 (seed de preços → vira job dev-only `make seed-prices`, gated no Makefile pelo mesmo discriminador dev do `make sync` — presença de `demo-data/<cliente>/*.json`; dentro do contêiner `ENVIRONMENT` é sempre `production`, então não serve de guarda), 004–011 (rewrites estruturais e null-backfills). Ids aposentados nunca são reutilizados | Decisão do usuário: o projeto parte do zero — não existe dado pré-convenção para migrar, e seed de dados não roda em produção (preços prod = exclusivamente `make price`, invariante 9). Os null-backfills eram higiene da convenção full-schema, já garantida em WRITE TIME pelo mapper (todo campo opcional nomeado → null na borda); os rewrites 006–009 eram caminhos de transição de layouts que nenhum deploy novo produz. De carona: 003 deixa de indexar as collections legadas (`spans`/`trace_contents`, extintas) e corrige o índice `agentId` (campo que não existe mais) → `agent.id`; constantes `LEGACY_*` removidas do repositório; `4-seed-demo-data.sh --prices` passa a rodar `make seed-prices` antes do modelo premium (o fluxo demo dependia do seed via migrate). Volumes locais existentes já aplicaram 001–011 — o runner ignora ids gravados; nada re-roda |
| 75 | Removido o connector register v1 (mock) — `packages/register/register.py` + `make register` (28/07/2026) | O martino-agent passou a falar SÓ o contrato de resolução por capability do connector-register real do khal-platform (`POST /connections` + token; serviço local em :7103, credenciais via semente dev do vault — sops do khal-platform). O documento hipermídia v1 (`GET → links{traces,events}`) não tem mais nenhum consumidor; mantê-lo seria um segundo contrato a divergir. Quem sobe o stack de observabilidade de um cliente registra o connector do LangWatch no register do khal-platform (`scripts/khal_register_connector.sh` no repo do agente) |
| 76 | Barra de filtros de traces (29/07/2026): os filtros `agent`/`type`/`channel`/`domain`/`subdomain` do GET /traces passam a aceitar múltiplos valores via query param repetido (`?agent=a&agent=b` — OR dentro do campo, AND entre campos; arrays do parser qs do Express, nunca string com vírgula); novo endpoint `GET /traces/filters` devolve as opções dos dropdowns — valores ARMAZENADOS por campo com contagem por opção (`{value, count}[]` em `domains`/`subdomains`/`types`/`agents`=agent.id/`channels`=channel.type/`statuses`, ordenados, nulls fora), em cascata com auto-exclusão: as opções do campo X honram todos os filtros exceto o do próprio X (um dropdown selecionado segue listando alternativas); cada `count` é um "e se" — traces que casariam com aquele valor combinado aos filtros dos OUTROS campos (refinado com o usuário na mesma sessão: contagem por valor + `statuses` na resposta, revertendo o "status não entra" da primeira versão). Migração 012 (`012-trace-filter-indexes`) cria `{channel.type, startedAt}` e `{subdomain, startedAt}` — distinct sem filtro só usa DISTINCT_SCAN com o campo como PRIMEIRA chave do índice; channel.type filtrava sem índice desde a decisão 42. O dropdown "Organização" do mock ficou de fora — não existe campo no trace (ver decisão 71); user/environment/experiment seguem sem filtro (decisão 70); sessões não mudam | Decisão do usuário na sessão de spec da barra de filtros: input em arrays (não CSV), opções processadas na API a partir do que existe no banco, cascata, e índices dimensionados para milhões de traces (QA15 anotada no repositório: fallback é pré-computar as opções sem filtro na ingestão se a latência das facetas apertar). O conjunto documentado de filtros do T10 já existia; esta decisão só muda a aridade e adiciona o endpoint de opções |
| 77 | API de traces sub-segundo em 1M+ traces (29/07/2026), medido em load test de 1M documentos: (a) migração 013 — todo índice de filtro termina na chave de ordenação COMPLETA `(startedAt: -1, traceId: 1)` (o tiebreaker fora do índice forçava COLLSCAN + sort em memória: 26s → 10-80ms) + índice novo `{domain, startedAt, traceId}` (o composto domain+subdomain bloqueia o sort para filtro só de domínio); (b) totais LIMITADOS na listagem — sem filtros usa contagem de metadados (exata aqui: escritor único), com filtros conta até 10.000 e para (`total_capped`, displays `total_display`/`total_pages_display` com sufixo "+"; *[**esta metade foi emendada pela decisão 116** (re-auditoria, iteração 2/3): o teto de 10.000 passou a valer TAMBÉM sem filtros — a promessa de exatidão acima do teto não existe mais]* contagem exata de combos arbitrários é O(docs casados): 10-29s medidos); (c) cubo de contadores `trace_filter_counters` — read-model DERIVADO (dia UTC × domain × subdomain × type × agentId × channelType × status, nulls na identidade da tupla), mantido na ingestão (increment no insert-once; delta no updateAttribution — invariante 7) e reconstruível por `make rebuild-filter-counters` (job = fonte de verdade do cubo; obrigatório uma vez em deployments restaurados/pré-existentes; NÃO entra na cadeia de migrações — decisão 74); GET /traces/filters agrega o cubo (tuplas minúsculas em vez de 1M documentos: 198s → sub-segundo), período em dias UTC inteiros (arredondado para fora; documentado), `search` cai no caminho live (ids exatos, poucos docs); zero-counts de deltas filtrados na leitura; índices `{dim, count}` no cubo tornam a faceta sem filtros um scan coberto index-only; cache TTL de 10s em memória no use case de opções (decoração de dropdown — staleness ≤10s aceito; desligado em teste para os asserts de ground truth); (d) `MONGO_MEMORY_LIMIT` no env do cliente (default 512m; cache do mongo dimensiona por cgroup — em 1M traces ≈ 3,4 GB, usar ≥4g) | Resposta parcial à QA15 com números medidos no dataset sintético de 1M (gerador `scripts/generate-loadtest-traces.mjs`): página 1 da listagem 9,6-26s, countDocuments({}) 29s, facetas em cascata 198s, single-pass $facet 24s — nenhuma forma de consulta que busca documentos fica sub-segundo em escala; o cubo é a resposta OLAP clássica. Billing NÃO lê o cubo (invariante 3: agregados ≡ soma dos carimbos, direto dos traces). Sessões ficam de fora ($group por request tem a mesma doença — follow-up próprio). Consistência cubo ≡ agregação live coberta por teste ground-truth na suíte |
| 78 | Escala horizontal do LangWatch (29/07/2026): a unidade de escala é o compute stateless (app + workers) sobre UMA espinha stateful (postgres/redis/clickhouse) por cliente. No compose: (a) `langwatch-workers` escala via `LANGWATCH_WORKERS_REPLICAS` (sem `container_name` — nome fixo limita a 1 réplica; coordenação pela fila do redis); (b) o app `langwatch` fica em 1 réplica (dono da porta publicada — escalar exige proxy fan-in, só quando o app for o gargalo); (c) limites de memória das três stores viram knobs de env (`LW_POSTGRES/REDIS/CLICKHOUSE_MEMORY_LIMIT`, `LANGWATCH[_WORKERS]_MEMORY_LIMIT`) — N réplicas multiplicam carga nas mesmas stores; (d) os três segredos `LW_*` ganham guard `:?` no compose (LangWatch pula validação de env e subiria com segredos VAZIOS — tokens forjáveis; com réplicas, sessões validam numa réplica e falham noutra; `CREDENTIALS_SECRET` tardio é irrecuperável); (e) o `trace-ingestion-worker` NUNCA escala além de 1 por fonte — o watermark não tem lease; `container_name` fixo mantido como trava deliberada + guard "nunca retrocede" no `setTraceCursor` (update condicional em ordem `(updatedAt, traceId)`; write rejeitado não é erro — o batch já foi persistido e `insertIfAbsent` deduplica re-leituras); (f) lista SCRUB do Makefile completada com TODAS as vars interpoladas pelos compose files (`UI_PORT`, `UI_IMAGE`, `MONGO_HOST_PORT`, knobs do worker e os novos de escala) — var exportada no shell do operador sobrepõe o `--env-file` de todos os clientes | Réplicas do LangWatch são invisíveis ao módulo (o ingestor lê ClickHouse, não o app); mais réplicas escritoras = mais clock skew alimentando `UpdatedAt` — dimensionar `TRACE_INGESTION_QUIET_PERIOD_SECONDS` com folga. Escala além de um host = derrubar o role file do connector e apontar `LANGWATCH_CLICKHOUSE_URL` para a stack externa (decisão 65). Identidade multi-fonte (`sourceId` por connector, cursor por fonte, unicidade `(sourceId, traceId)`) fica como decisão futura — registrar ANTES de dados de produção para evitar backfill *[**Refinada pela decisão 139 (10/08/2026)**: os cinco knobs de capacidade viram OBRIGATÓRIOS sem default no compose — capacidade é decisão declarada]* |
| 79 | Pacote de robustez pós-auditoria (29/07/2026): (a) sessões ganham o tratamento de 1M — acumulador `$top` substitui o pre-sort bloqueante inindexável (a direção `{startedAt:1, traceId:1}` não é servida por índice nenhum: em 1M o estágio estourava 100 MB e o endpoint devolvia 500), narrowing em duas fases para filtros de período (candidatas = sessões ATIVAS na janela via índice; agregação só das candidatas com somas COMPLETAS; janela QA17 exata aplicada pós-group), `allowDiskUse`, totais limitados como traces (`total_capped`/displays "+"), cadeia do detalhe limitada a 1.000 traces com `chain_truncated` explícito (nunca truncar em silêncio) — read-model materializado segue como follow-up; (b) horizonte único de paginação `MAX_PAGINATION_SKIP` (10.000): a página que implicaria skip além do horizonte é 400 (contagem limitada sem skip limitado deixava `?page=50000` como DoS de index-walk); (c) guard QA14 no cliente HTTP: janela com mais hits que uma página ABORTA alto (pageOffset é ignorado pelo search — o loop antigo reportava duplicatas como sync saudável enquanto o excedente se perdia para sempre); (d) quiet period (decisão 61) também no caminho de janela (`make sync`): clamp do teto para `now − quiet` nos DOIS clientes reais (fake não clampa — fixtures são estáticas); trace em voo carimbado parcial é imutável = subfaturamento permanente; (e) circuit breaker de poison: batch inteiro (≥10 linhas) todo inválido = drift de schema, não linhas isoladas — lança SEM avançar o cursor (decisão 62 continua valendo abaixo do limiar); fecha também o poll sem sleep do batch cheio todo-poison; (f) correção manual de atribuição carimba `attributionCorrectedAt` (convenção de runbook) e `updateAttribution` (refresh do source) PULA documentos marcados — sem isso, qualquer re-sync (ou o crash-replay do próprio loop) revertia a correção para o valor errado do source; (g) varredura de pendentes com projeção slim (traceId/model/startedAt/tokens — payloads embutidos ficavam gigabytes em memória no pior momento: incidente de modelo sem preço) + isolamento por trace com contador `failed` (um trace com erro não perde mais a varredura; `skipped` de racer conta como stamped, que é o que ele é); (h) contagens de tokens inteiras e não-negativas NA FRONTEIRA nos três clientes (fração passava e travava o stamper deterministicamente = livelock do sync; negativo cunhava carimbo R$ 0,00 fabricado); (i) `insertIfAbsent` captura E11000 → 'skipped' (worker + `make sync` concorrentes derrubavam o batch inteiro); (j) view de billing soma os cents reconciliados direto (`partsCents`) em vez de re-parsear a string formatada via float — único ponto de dinheiro-float do código, eliminado | Cobertos por testes: contrato de correção protegida, breaker/limiar, guard QA14 (1 chamada, aborta antes dos details), clamp de quiet period (helper puro), profundidade de página (3 casos no controller), fração/negativo no mapper. CORS `*`, bind 0.0.0.0 e mongo sem auth ficam como DECISÃO DE POSTURA pendente (exposição confiável vs. knobs de rede) — não código |
| 80 | Read-model MATERIALIZADO de sessões (29/07/2026): coleção `session_summaries` (1 doc pequeno por sessão), mantida por RECOMPUTE-ON-TOUCH — todo write que toca uma sessão (insert, stamp de pendente, mudança de atribuição) re-deriva o resumo DAQUELA sessão via a MESMA pipeline (agregação indexada por sessionId, custo limitado pelo tamanho da sessão). Exato por construção (o recompute lê o store DEPOIS do write) e auto-curável (crash entre write e recompute deixa a sessão stale só até o próximo toque ou o rebuild) — a classe de drift dos contadores incrementais (delta perdido = errado para sempre) não existe aqui. `GET /sessions` vira find indexado na coleção materializada (migração 014: índices terminando na sort key completa `{startedAt:-1, sessionId:1}`, regra da decisão 77); o DETALHE continua derivado ao vivo (1 agregação por sessão — o drill-down que carrega dinheiro nunca fica a uma janela de staleness do store); a pipeline de derivação existe UMA vez (`session-summary-pipeline.ts`), consumida por recompute, rebuild e detalhe — os três leitores não podem divergir (invariante 3: custo da sessão ≡ Σ carimbos segue por construção). `make rebuild-session-summaries` (job fonte-de-verdade, obrigatório uma vez em deployments restaurados — mesmo contrato do cubo da 77). Convenção de runbook: correção manual direta no banco deve rodar o rebuild depois (edits crus não passam pelos hooks). Substitui o stopgap da decisão 79(a); billing segue NÃO lendo nenhum read-model derivado | Teste ground-truth com somas recomputadas à mão nas fixtures (não via a pipeline sob teste), equivalência rebuild ≡ manutenção incremental, e as rotas de sessões passam pelo caminho materializado ponta-a-ponta (harness ingere via use case real → hooks alimentam a coleção) |
| 81 | Contadores de faceta em TRANSAÇÃO (29/07/2026): trace + increment do cubo comitam ou abortam JUNTOS (`MongoDb.withTransaction`), e o read-modify-write inteiro do `updateAttribution` (snapshot → merge → delta → unclassified) vira uma transação só — a classe de drift "increment perdido entre dois writes = errado para sempre" (achado da auditoria; batch re-lido vê 'skipped' e nunca re-incrementa) deixa de existir na raiz, dispensando detecção de drift. Pré-requisito: transação multi-documento exige replica set — o mongo do compose vira REPLICA SET DE NÓ ÚNICO (`--replSet rs0`): upgrade in-place suportado (volume standalone existente preservado; healthcheck faz `rs.initiate` uma vez, membro pinado no nome de SERVIÇO `mongo:27017`, e só reporta healthy quando primary gravável); com auth, RS exige keyFile — gerado no volume no primeiro boot autenticado e reutilizado. Recompute de sessões (decisão 80) fica DELIBERADAMENTE fora da transação (auto-curável; transação de 2 writes fica rápida e sem conflito); rebuilds ($out não é permitido em transação) continuam fora — agora só necessários para restores, não para reparo de drift. Jest memory-server configurado como RS de nó único (`jest-mongodb-config.js`) — as suítes exercitam os MESMOS writes transacionais de produção. Mongo EXTERNO (decisão 65) passa a exigir replica set (Atlas/gerenciados sempre são). Compass/mongosh do host: `?directConnection=true`. Smoke-testado nos dois modos (auth-less e auth+keyFile): initiate, primary, transação comitada | Deployments existentes: um restart do mongo com o novo command basta (dados preservados); custo por write = commit de transação em RS de nó único — desprezível nas taxas de ingestão medidas |
| 82 | Modelo como REF ESTRUTURADO no domínio (29/07/2026): `TraceModel.model` deixa de ser string solta e vira `{ id, provider }` (`domain/models/model-ref.ts`), parseado UMA vez na ingestão (`parseModelRef`: split no primeiro `/`; id puro ganha provider inferido por prefixo conhecido — claude→anthropic, gpt→openai, gemini→google, … — ou `provider: null`, honestidade sobre chute). O FIO permanece a string `provider/id` recomposta por `modelKey` (compatibilidade total com o front e com o formato da fonte); a MESMA chave canônica alimenta o lookup da tabela de preços (T4 continua indexada por string) e o agrupamento do billing — fio, preço e billing consistentes por construção. Efeito colateral deliberado: id puro com provider inferido passa a casar com a linha de preço canônica (`google/gemini-2.5-pro`), onde antes caía pending_price. Migração 015 (`015-model-object`) reescreve traces armazenados com model string para o bloco canônico — a PRIMEIRA exceção à cadeia indexes-only da decisão 74 (idempotente: filtro `$type: 'string'`); o carimbo permanece intocado (invariante 1) | Front não muda (string no fio); novos providers/formatos = editar só `model-ref.ts`; agrupamento do billing agrupa pelo objeto e recompõe a chave na borda |
| 83 | Endpoint de registro de preço (29/07/2026): `POST /api/v1/prices` registra uma NOVA versão (T4) — corpo `{model, token_type, price_brl_per_million, effective_from}`, contrato ESTRITO (campo desconhecido = 400, typo nunca registra preço errado), dinheiro como STRING decimal convertida para µ¢ inteiros na borda (nunca float). Caminho ÚNICO compartilhado com o job de runbook `price:insert` (`RegisterPriceVersionToDbUseCase`): canonicaliza o modelo para a chave `provider/id` do carimbo (decisão 82) e dispara o reprocess imediato (decisão 57) — o relatório (`examined/stamped/still_pending/failed`) vem na resposta 201. Duplicata de (model, token_type, effective_from) = 409 (invariante 9; `DuplicatePriceVersionError` MOVIDO para `domain/errors/` — é parte do contrato do repositório e a presentation não pode importar application). As colunas internas de margem (US$/PTAX/markup) ficam FORA do contrato HTTP (espírito da invariante 4 — o OpenAPI é servido publicamente); captura de margem continua no job. Atualiza a invariante 9 do CLAUDE.md ("inserts diretos" → endpoint ou job, um caminho só) | Preço já aplicado nunca muda (invariante 1): a versão nova só vale para carimbos futuros + pendentes desbloqueados; docs OpenAPI geradas do MESMO schema zod do request (io: input) |

| 84 | Auth M2M gateada por env na API (30/07/2026): com `AUTH_SYSTEM_URL` setada, todo request a `/api/v1` exige `Authorization: Bearer <token M2M>`, validado por INTROSPECTION no Auth System do khal (RFC 7662 — só `active` é lido, fail closed em erro/timeout/não-200; preflight OPTIONS e `/api/v1/docs` ficam abertos, docs = healthcheck). O `/introspect` é protegido (RFC 7662 §2.1): o module autentica-se com a PRÓPRIA credencial M2M (`AUTH_SYSTEM_CLIENT_ID`/`AUTH_SYSTEM_CLIENT_SECRET`, Basic) — smoke contra o Auth System real confirmou `invalid_client` sem ela. O module NÃO carrega lógica de scope nem tenant — segue a simplificação M2M da plataforma (claims `{tenant, client_id, client_secret}`, sem scopes; supersede a proposta JWKS+escopo do achado #5 de `khal-os-inconsistencies.md`). Sem a env, API aberta (compat PoC). Porta `TokenAuthenticator` em `application/interfaces/`, adapter `infrastructure/auth/http-token-authenticator.ts`, middleware em `main/server/middlewares/auth.ts` (responde 401 direto — o error boundary achataria 4xx de middleware em 400). Pendências: UI ainda não envia token (ligar auth só em deployments API-only) e CORS segue wildcard | Ligar/desligar é 1 env var por cliente; a UI ganha o token numa fatia própria |
| 85 | Scripts do connector LangWatch MOVIDOS para este repo + versionamento por artefato (30/07/2026): o connector é provisionado por este módulo (`compose.connector.yml`, onboard-langwatch), então seus scripts moram aqui — `scripts/connector/{version,bump-version.sh,register.sh}` (o `khal_register_connector.sh` do martino-agent foi deletado; atualiza o pointer da decisão 75). O manifest agora envia `version` (SemVer, fonte: arquivo `scripts/connector/version`) e NÃO envia mais `requiredScopes` (SPEC-3 da plataforma remove scopes do modelo M2M; campo opcional no schema atual — remoção segura). O módulo ganhou o mesmo par: `scripts/bump-version.sh` (package.json root+api+ui em sincronia) e `scripts/register-module.sh` (PUT `/modules/tracing` com `version` lida do package.json; substitui o curl manual do RUNBOOK-PLATFORM). Registers são a fonte de verdade da versão implantada — re-registrar após todo bump/deploy | Tokens dev dos scripts ainda carregam scopes (marcados LEGACY até a plataforma remover a checagem); `TOKEN` env aceita um token de usuário real |

| 86 | domain/subdomain → topic/subtopic: tentada e REVERTIDA no mesmo dia (30/07/2026). Motivação original: `topic.id`/`subtopic.id` seriam chaves "nativas" do LangWatch. Verificação empírica na 3.5.0 derrubou o ganho: as chaves chegam verbatim no Attributes/metadata — mesmo transporte dos custom keys `domain`/`subdomain` — e as colunas TopicId/SubTopicId do LangWatch são preenchidas pelo topic clustering automático (worker próprio, precisa de configuração), NUNCA por esse metadata. Ou seja: rename interno de ~40 arquivos sem nenhum efeito observável (o contrato HTTP já era `domain`/`subdomain` e continuaria). Revert completo nas DUAS pontas antes de qualquer commit: módulo segue `domain`/`subdomain` em todo lugar (modelos, repos, cubo, Mongo, fio) e o martino-agent volta a emitir `domain`/`subdomain` bare keys (envs `DOMAIN`/`SUBDOMAIN`). A migração 016 ($rename) foi descartada junto — id 016 aposentado, cadeia segue indexes-only (decisão 74, única exceção: 015). Sem dados em produção, então sem rastro | Se um dia quisermos as colunas TopicId do LangWatch, o caminho é configurar o clustering worker — não renomear metadata |
| 87 | T6 entra: ciclo de vida do mês SÓ por runbook na v1 (31/07/2026 — implementação overnight dos épicos 5–8; QA4 respondida: gatilho é admin/runbook, sem automático). `make billing-close CLIENT=x YEAR=y MONTH=m` e `make billing-reopen ... REASON='...'` (REASON obrigatório — auditado). Sem endpoint HTTP de mutação: o modelo M2M não tem papéis (decisão 84), então um POST /close ficaria exposto ao tenant inteiro. Fechamento exige mês-calendário completamente no passado (UTC, invariante 8) e é BLOQUEADO com trace `pending_price` no mês (o job lista os modelos sem preço). Concorrência: flip do período é update guardado + índice único — dois closes simultâneos não vencem os dois. A saída do job É a notificação da US5 (alertas/notificações fora do escopo v1 pela planilha) | Verificado ao vivo: close bloqueado com 2 pendentes → preço registrado (decisão 57 reprocessa na hora) → close ok |
| 88 | Snapshot T6 = ENTRADA + SAÍDA + auditoria, e UM MOTOR só (31/07/2026): `statement-engine.ts` (função pura) é A conta do billing — o mês aberto roda ele ao vivo a cada leitura, o fechamento roda ele uma vez e congela entrada (usage records por trace, carimbos copiados verbatim — coleção própria `billing_snapshot_usage`, um doc por trace, longe do teto de 16MB) e saída (`statement` client-facing: linhas, agregados, %, mix, cache, centavos reconciliados) + `logicVersion`/`roundingRule`/watermark/priceVersionsApplied. Reprodutibilidade (critério de aceite T6) é teste automatizado: motor sobre os inputs do snapshot ≡ output gravado, ao µ¢ e ao centavo exibido — inclusive via storage real (Mongo). Reabertura preserva TODAS as versões; re-fechamento grava v+1. Invariante 3 vira propriedade mecânica: fechado ≡ ao-vivo-no-instante-do-fechamento por construção | `billing_periods` (docs de ciclo de vida, audit append-only) + `billing_snapshots` + migração 017 (indexes-only) |
| 89 | Pós-fechamento (31/07/2026): trace datado em mês fechado é ARQUIVADO (invariante 6 — store everything) com flag `billingQuarantine`, fora da fatura congelada; `reprocess:pending` PULA meses fechados (contador `blocked_closed_month` no relatório e na resposta do POST /prices); refresh de atribuição também bloqueado em mês fechado (correção só via reabertura auditada — invariante 7 vale para períodos ABERTOS). Visibilidade admin: contagem de quarentena em /bills e no extrato + relatórios dos jobs | A quarentena marca; nunca apaga. Reabrir o mês re-inclui os quarentenados no próximo fechamento (v+1) — esse É o fluxo de correção |
| 90 | Extrato evoluído EM LUGAR, sem endpoint paralelo (31/07/2026): GET /billing/summary agora serve mês fechado EXCLUSIVAMENTE do snapshot (nunca recalcula — período fechado sem snapshot é estado corrompido e responde erro, não um recálculo silencioso) e mês aberto ao vivo pelo mesmo motor. Linhas US8 ganham preço unitário aplicado: o agrupamento passa a incluir o preço (agente × versão × modelo × tipo × preço × vigência) — troca de preço no meio do mês gera LINHAS SEPARADAS, cada linha continua literalmente `quantidade × preço = custo`. % por agente em basis points inteiros reconciliados (largest remainder, soma exata 10000). Comparação US10 derivada NA LEITURA dos dois meses (referencia outro mês — nunca entra em snapshot). /bills ganha status closed/in_progress/open + números congelados p/ fechados | Rótulos US6: "Fechado — final" / "Em andamento — dados parciais" / "Aberto — aguardando fechamento" |
| 91 | Economia de cache (T9, QA7 respondida como EXPLÍCITA) (31/07/2026): contrafactual = tokens de cache_read agregados POR PREÇO DE INPUT aplicado no próprio trace × esse preço (half-up no µ¢, determinístico); economia bruta = contrafactual − custo real de leitura; escrita de cache é custo real mostrado À PARTE; líquida = bruta − escrita (pode ser negativa). Trace com cache_read sem preço de input carimbado fica FORA do contrafactual e é contado (`unpriceable_cache_read_traces`) — honestidade sobre a base da conta. Métrica derivada: nunca entra na fatura | Exibição com precisão exata de linha (não arredonda a R$ 0,00 em volumes pequenos) |
| 92 | Série mensal (T8/US11) (31/07/2026): GET /billing/series — UM total por mês em todo lugar (fechado = total do snapshot, batendo com o extrato para sempre; aberto = soma viva; corrente marcado in_progress). Séries total/por agente/por modelo com geometria de barra PRÉ-COMPUTADA (height_percent numa escala única — a mais alta = 100), UI segue camada burra de render (decisão 51); dimensão de agente da série é o id (versões fundem). Histórico nasce raso (~49 dias de backfill) e engorda | Mês corrente desenhado tracejado/âmbar na UI — parcial à primeira vista (US6) |
| 93 | Projeção (T8/US12) (31/07/2026): GET /billing/projection — run-rate linear documentado em PALAVRAS na resposta (`basis_text`): acumulado dos dias UTC COMPLETOS ÷ dias completos × dias do mês (inteiros, BigInt, half-up; o dia de hoje nunca dilui a taxa). Só responde o mês corrente (sem parâmetro de mês — projeção de mês passado é contradição); <3 dias completos → `insufficient_data`, número nenhum é inventado; NUNCA persiste, some no fechamento. Card na UI com borda tracejada + rótulo "estimativa — não é fatura" | |
| 94 | Exportação US17 v1 (31/07/2026; QA13 respondida: mês corrente EXPORTA com marca d'água): GET /billing/statement.csv (linhas US8 + cabeçalho mês/status/timestamps; UTF-8 com BOM p/ Excel pt-BR; ';'; mês parcial → coluna e filename PARCIAL) e GET /billing/statement/print (HTML standalone imprimível — Ctrl+P → PDF; fechado = status final + timestamps do snapshot; corrente = marca d'água PARCIAL atravessada). XLSX e PDF binários ADIADOS: política de zero dependências novas no módulo — CSV abre no Excel e o print view vira PDF no navegador. Renderização única: os dois exports derivam do MESMO view do extrato (fechado ⇒ snapshot verbatim) | Adapter HTTP ganhou resposta raw com headers (Content-Disposition) — mudança mínima no express-route-adapter |
| 95 | Fronteira de mês segue UTC (31/07/2026 — flag para alinhamento): toda a implementação usa o `monthWindowUtc` existente (mês-calendário UTC). Cliente fatura em horário BR — se produto decidir América/São_Paulo, a mudança é concentrada em `monthWindowUtc` (e nos $year/$month UTC dos pipelines), com snapshot já fechado NÃO re-agrupado (versões preservam a regra da época). PERGUNTAR amanhã | Nada no código depende da resposta além dessa função |
| 96 | `pricingType` na versão de preço — a costura para precificação COMPUTADA (31/07/2026, pedido do Matheus): toda versão declara como seu R$ é RESOLVIDO. Hoje só `'fixed_brl'` (o preço É o valor declarado); um tipo futuro (ex.: `'usd_ptax_markup'` — USD × PTAX-do-dia × markup) entra como novo resolver + novos campos declarados na própria versão, com PTAX como DADO versionado no store (nunca API viva — re-carimbo precisa ser reprodutível). O despacho já é real: `findEffectivePrices` só resolve tipos conhecidos — versão de tipo desconhecido ⇒ SEM preço efetivo ⇒ trace `pending_price` (invariante 2, nunca custo chutado). O carimbo continua sendo a fronteira: grava o R$ RESOLVIDO — motor/extrato/snapshot nunca sabem como o preço nasceu (invariante 1 intacta; billing não muda). REMOVIDAS as colunas internas `marketPriceUsd`/`ptaxReference`/`markupPercent` (T4 as capturava "para análise futura de margem" — com pricingType, cada tipo declara os SEUS campos; as colunas soltas eram peso morto): fora do modelo, do job (`--market-usd/--ptax/--markup`), do seed e do script demo. Linhas antigas no store: `pricingType` ausente = fixed_brl (default legado, sem migração — cadeia segue indexes-only); campos null remanescentes são inertes. Consequência ciente p/ preço diário: linhas do extrato agrupam por preço aplicado (decisão 90) ⇒ até ~30 linhas/mês por tupla — exato e auditável; rollup de exibição se ficar denso | Preço diário vira: declaração imutável + tabela PTAX + resolver puro |
| 97 | Série empilhada por tipo de token + lente DIÁRIA (31/07/2026, pedido do Matheus): toda barra da série (mensal e diária; total, por agente e por modelo) vira pilha de segmentos input/output/cache leitura/cache escrita, nas MESMAS cores das linhas do extrato; geometria pré-computada na API (`height_percent` do gráfico + `stack_percent` de cada segmento — UI segue render puro, decisão 51). Fechado: split somado dos números CONGELADOS do snapshot (linhas/costByTokenType), nunca recomputado. Nova granularidade `GET /billing/series?granularity=day&days=N` (default 30, limite 1-90 — presets 7/30/90 do protótipo; mensal segue default 12, teto 24): mesmos carimbos em baldes de dia UTC, janela termina HOJE (sempre parcial, listrado), dias vazios viram barra zero (gap TEM que parecer gap), lente diária é só a série total (toggles de agente/modelo ficam na mensal). Quarentenados EXCLUÍDOS do rollup diário: os dias de um mês fechado somam exatamente a fatura congelada — a lente diária é operacional/analítica, a fatura continua mensal | UI: toggle Meses/Dias + presets 7/30/90 + legenda de tipos sob o gráfico |
| 98 | Exportação do extrato RESTful (31/07/2026, pedido do Matheus): os dois endpoints de export (`/billing/statement.csv` e `/billing/statement/print`) viraram UM recurso — `GET /billing/statement?year&month&format=csv\|html` (`csv` default = dados US8 p/ Excel; `html` = imprimível, Ctrl+P → PDF; format inválido → 400). Um controller só, os dois renderers derivam do MESMO view do extrato (fechado ⇒ snapshot verbatim; corrente ⇒ marca PARCIAL, QA13). Rotas antigas REMOVIDAS sem depreciação — nada externo consumia (nasceram e morreram no mesmo dia); UI/OpenAPI/Postman atualizados. Supersede o desenho de rotas da decisão 94 (o conteúdo/marca d'água dela segue valendo) | Formato novo (ex.: xlsx nativo) = novo valor de `format`, não nova rota |
| 99 | Flakiness do `npm test` erradicada na raiz (31/07/2026, investigação sob carga): DUAS causas reproduzidas e corrigidas. (a) O teste de rebuild do cubo de facetas ordenava linhas por `JSON.stringify(doc)` — mas o mongod NÃO garante ordem de campos BSON em documentos criados por upsert (varia por operação, sensível a carga) e o rebuild via `$out` grava outra ordem; a chave de ordenação virou a PRÓPRIA tupla de dimensões (comparação livre de locale e de ordem de campos). (b) O `testEnvironment` do preset jest-mongodb relia `globalConfig.json` antes de CADA suíte; um segundo jest sobreposto (ou abortado) apaga esse arquivo no globalTeardown e derruba TODAS as suítes restantes — inclusive as sem Mongo (o colapso observado de 46/50). `jest.config.mjs` agora fixa `testEnvironment: jest-environment-node` (as suítes leem `process.env.MONGO_URL`, que o globalSetup do preset define no processo; o environment do preset não fazia falta). Validação: 40 execuções da suíte antes-flaky + 10 execuções completas consecutivas, tudo verde sob carga sintética (CPU+fsync+churn de portas) que reproduzia a falha ~1/25 | Rodadas concorrentes de `npm test` (duas sessões/worktrees) agora são seguras; NÃO reverter o override do testEnvironment ao atualizar o preset |
| 100 | Ciclo de vida da quarentena — o snapshot ADJUDICA (01/08/2026, auditoria completa do repo, execução aprovada pelo Matheus): a flag `billingQuarantine` (decisão 89) deixa de ser write-once. Após o flip do fechamento, `reconcileQuarantineAfterClose` (dois `updateMany` idempotentes): trace FATURADO pelo snapshot ganha `billingQuarantine.absorbedInSnapshotVersion` (absorvido = dentro da fatura; a marca histórica NUNCA é apagada — trilha de auditoria), e retardatário que entrou na corrida ingestão×fechamento e ficou FORA do snapshot é flagrado mecanicamente, qualquer que seja o interleaving. Leitores (`dailyRollup`, `countQuarantined`) passam a considerar SÓ quarentena não resolvida (sem `absorbedInSnapshotVersion`) — restaura as invariantes 3/8 no fluxo de correção da 89 (reabrir→re-fechar), provado por teste de aceite automatizado: quarentenado → reopen → re-close ⇒ Σ rollup diário ≡ total do snapshot v2 ∧ `countQuarantined` = 0. `/bills` reporta contagem de quarentena real também em mês aberto *[**Refinada pela decisão 113**: no `dailyRollup` a exclusão vale só DENTRO das janelas de meses FECHADOS — em mês aberto ou reaberto a lente diária mostra tudo que o extrato ao vivo cobra]* | Absorvido ⇒ faturado, com a marca preservada; sem a adjudicação, a garantia da 97 (dias de mês fechado ≡ fatura congelada) quebrava para sempre em todo mês corrigido |
| 101 | Ingestão NUNCA descarta e NUNCA trava (01/08/2026, auditoria completa do repo, execução aprovada pelo Matheus): falha determinística de UM trace não bloqueia mais o batch — dead-letter durável `ingest_failures` (por trace, com cursor e erro) + `poison_rows` (linha venenosa registrada no ATO do skip, no mesmo store do arquivo — log de contêiner rota, a trilha da decisão 62 agora é permanente); breaker quando o batch INTEIRO falha (pane de store ≠ poison: lança SEM avançar o cursor). Trace acima de ~15MB BSON entra com conteúdo de span truncado (`{truncated, originalBytes}`) e flag explícita `contentTruncated` no trace — a ÚNICA concessão sancionada à invariante 6: o trace nunca é descartado, o conteúdo gigante pode ser truncado com trilha; tokens e custos intocados (derivam das contagens, não do conteúdo). Divergência de tokens em re-sync `skipped` (trace retomado após a quarentena de 15 min) é logada/contada (`tokenDivergence`) — nunca muda contagem nem carimbo (invariante 1) *[**Emendada pelas decisões 110/111** (re-auditoria, iteração 1) **e por dois ajustes do mesmo passe**: (a) trace que continua acima do teto DEPOIS de clipar conteúdo de span, conteúdo de trace E `errorMessage` de span não tem forma armazenável — NÃO é inserido e vai para dead-letter com o kind honesto `oversized_unstorable`, sem NENHUM registro de truncamento; o registro de truncamento passou a ser escrito só APÓS o insert bem-sucedido, porque "truncado" só se diz de trace que está no arquivo; (b) o dead-letter deixou de ser silencioso — o worker imprime a contagem parada a cada ciclo (quando > 0) e o README traz o runbook de recuperação]* | Head-of-line blocking virando perda permanente de arquivo (retenção ~49d) deixa de existir; a varredura de reprocess roda na cadência própria, independente do drain |
| 102 | Ids de modelo em minúsculas no ponto único de canonicalização (01/08/2026, auditoria completa do repo, execução aprovada pelo Matheus): `parseModelRef` passa a rebaixar também o `id` (o provider já era) — `Anthropic/Claude-X` e `anthropic/claude-x` viram a MESMA chave de preço/billing; antes, variante de caixa caía em `pending_price` silencioso que registrar o preço "como o operador o conhece" não destravava. Migração 019 backfilla `traces.model` + chaves de `price_versions` (atribuição apenas — carimbos intocados, invariante 7; snapshots congelados mantêm as chaves da época). Política de colisão sob o índice único: o registro MAIS ANTIGO fica com a chave canônica; variantes posteriores ficam INERTES e logadas — nunca apagadas (invariante 9: versões imutáveis) | Segunda exceção (com a 015) à cadeia indexes-only da decisão 74 |
| 103 | `/api/v1/docs*` + `openapi.json` seguem ABERTOS sob auth (01/08/2026, auditoria completa do repo, execução aprovada pelo Matheus): são o healthcheck do contêiner e a superfície de integração (o register-module aponta o health para o `openapi.json`); o bypass do preflight OPTIONS idem. Comportamento deliberado da decisão 84 que só existia em comentário — agora registrado; suaviza o "todo request a /api/v1 exige token" | Gatear o `openapi.json` exigiria um `/health` dedicado — só se a postura mudar |
| 104 | Preço ZERO rejeitado no registro (01/08/2026, auditoria completa do repo, execução aprovada pelo Matheus): `price_brl_per_million: "0"` responde 400 nos DOIS caminhos (HTTP + runbook — mesmo use case, decisão 83), e a regex ganhou teto de dígitos (overflow vira 400, não 500). Um zero acidental disparava o reprocess imediato (decisão 57) e carimbava todo pendente do modelo a R$ 0,00, imutável — a violação mais cara do espírito da invariante 2 ("nunca valorado a zero"). Free tier real, se um dia existir, é decisão NOVA no log — nunca default silencioso | |
| 105 | Bindings de host em LOOPBACK por default (01/08/2026, auditoria completa do repo, execução aprovada pelo Matheus): api, ui e langwatch publicam em `127.0.0.1:<porta>` — do host, alcançáveis só via localhost; expor além disso é ato explícito do operador (`API_BIND`/`UI_BIND`/`LANGWATCH_BIND`, documentadas no example.env). Motivo: API auth-less por default + signup aberto do LangWatch expunham o arquivo integral sem máscara a qualquer interface de rede. A UI alcança a API pela rede interna do compose — dashboard intacto | Acesso via LAN passa a exigir `*_BIND=0.0.0.0` + firewall — mudança consciente de comportamento de deploy |
| 106 | `MONGO_DB_ATLAS` consertada como string-boolean (01/08/2026, auditoria completa do repo, execução aprovada pelo Matheus): o schema era `z.boolean()` sobre `process.env` (sempre string) — QUALQUER valor setado derrubava o boot (`process.exit(1)`); o branch Atlas era código morto, documentado como armadilha no Dockerfile em vez de consertado. Agora `'true'/'false'` com transform; branch mantido | |
| 107 | UI sob auth mostra 401 HONESTO (01/08/2026, auditoria completa do repo, execução aprovada pelo Matheus): com auth ligada, a UI respondia "A API do cliente está no ar?" — mensagem de outage para uma condição de auth; agora responde a mensagem dedicada "autenticação ativa — esta UI ainda não envia token". O fluxo real de token da UI (injeção de service-token no nginx) é trabalho futuro deliberado, fora deste passe | Auth segue recomendada só para deployments API-only (decisão 84) |
| 108 | Registro retroativo: `GET /api/v1/sessions/filters` (01/08/2026, auditoria completa do repo, execução aprovada pelo Matheus — o commit 78d4e41 citava "decisão 76/80" sem entrada própria no log): dropdowns `agents` + `statuses` para a lista de sessões, em cascata com auto-exclusão como os de traces (decisão 76), servidos do read-model materializado (decisão 80) | Sem mudança de comportamento — o log fica completo |
| 109 | `STATEMENT_LOGIC_VERSION` → `statement-engine/2` (01/08/2026, auditoria completa do repo, execução aprovada pelo Matheus): a matemática de shares/basis points do motor migrou de float para BigInt (floors exatos, largest remainder inteiro, acumulações de grupo por soma assertada de µ¢) — resultados idênticos nas magnitudes reais (a imprecisão float só aparecia em escala extrema), então snapshots existentes seguem reproduzíveis; o bump segue a regra do próprio motor: QUALQUER mudança de conta = nova `logicVersion` | Fechamentos futuros gravam `statement-engine/2`; snapshots antigos guardam a versão da época |
| 110 | Salvamento de linha ClickHouse SÓ quando os spans reconstroem a contagem (01/08/2026, re-auditoria da execução, iteração 1 — invariante 2): a regra C-6.2 anulava contagens de token corrompidas (negativas/fracionárias) e deixava o trace seguir; quando os spans não traziam `gen_ai.usage.*`, o trace chegava ao carimbo com ZERO tipos de token usados ⇒ nenhum preço faltante ⇒ carimbo IMUTÁVEL de R$ 0,00, inalcançável pelo reprocess (trace sem tipo de token usado não tem preço a esperar) — exatamente o desfecho que a invariante 2 proíbe. A regra passa a ter duas metades: a fronteira (`clickhouse-row-schema.ts`) apenas anula e REPORTA os campos (`nulledTokenFields`); o cliente (`clickhouse-langwatch-client.ts`, `salvageIsSafe`) só deixa passar se `unreconstructedTokenFields` vier vazio — isto é, se as somas de `gen_ai.usage.*` dos spans reconstruíram TODAS as contagens anuladas. Caso contrário a linha vira poison durável e o cursor avança sobre ela como sobre qualquer poison (decisão 62): registro de poison é melhor que carimbo imutável errado. Todo salvamento — inclusive o SEGURO — grava registro durável `kind: 'summary_salvaged'` em `poison_rows`, porque console.warn não é trilha (C-6.2) | Consequência ciente: um lote inteiro (≥10 linhas) de salvamentos RECUSADOS tripa o breaker de poison (decisão 79) e para o avanço do cursor — sinal de defeito de instrumentação na fonte, não de drift de schema |
| 111 | Falha de INFRAESTRUTURA no ingest é ABORTO, não dead-letter (01/08/2026, re-auditoria da execução, iteração 1 — segurança do cursor): o breaker "todos falharam" da decisão 101 só vale para lotes ≥10, e um worker em dia processa lotes de 1–9 — uma falha sistêmica de escrita (primário rebaixado, capacidade transacional perdida, documento de contador corrompido) dead-letterava trace a trace e levava o watermark adiante por dias, com a escrita do próprio dead-letter funcionando o tempo todo. Agora a CLASSIFICAÇÃO decide (`isSystemicStoreError`, em `trace-ingestor.ts`): nomes de erro do driver, códigos retryable documentados e labels `TransientTransactionError`/`UnknownTransactionCommitResult` — duck-typed de propósito, para a camada de aplicação seguir cega a storage (decisão 56) — fazem o erro SUBIR e o lote abortar sem mover o cursor; falhas com forma de TRACE (BSON grande demais, validação, overflow de dinheiro) seguem para dead-letter. `ExceededTimeLimit` (262) fica DE FORA de propósito: pode ser um documento enorme, e esse trace precisa continuar dead-letterável. O breaker ≥10 fica como segunda rede para o que o classificador não nomear | Troca deliberada: um abort de contenção custa um lote refeito (o backoff do worker paga) em vez de um trace estacionado por engano — buraco no arquivo que ninguém percebe |
| 112 | Fechar um mês EXIGE que os meses mais antigos com traces estejam fechados (01/08/2026, re-auditoria da execução, iteração 1): `firstOpenMonthStart` (o limite C-7.1 do scan ao vivo) caminha do mês fechado mais antigo para frente — assume disciplina de fechamento do mais antigo para o mais novo, e nada a impunha. Fechar junho com um maio nunca fechado e COM traces jogava maio para trás do limite: o dinheiro de maio sumia de `/bills` e da série mensal enquanto `GET /billing/summary?month=5` continuava mostrando — dois endpoints da MESMA verdade discordando (invariante 3). Agora `assertOlderMonthsClosed` varre do trace mais antigo até M-1 e bloqueia com `BillingCloseBlockedError` nomeando o mês a fechar primeiro (mês genuinamente sem tráfego passa). No mesmo passe a reconciliação pós-fechamento ganhou caminho de REPARO — rodar `make billing-close` num mês já fechado re-executa a reconciliação idempotente a partir dos ids de uso do snapshot vigente ANTES de recusar (um crash entre o commit do fechamento e a reconciliação deixava o mês fechado-e-não-reconciliado para sempre) — e as duas passadas pararam de mandar o mês inteiro de ids num comando só: os retardatários são apurados client-side por cursor projetado e ambas escrevem por `$in` fatiado (teto de 16MB do documento de comando) | Fechamento fora de ordem deixa de existir pela porta do runbook; recuperar um fechamento interrompido é rodar o MESMO `make billing-close` de novo |
| 113 | A exclusão de quarentena não resolvida vale só DENTRO de meses fechados (01/08/2026, re-auditoria da execução, iteração 1 — refina a decisão 100): a 100 aplicava o filtro em TODO `dailyRollup`; num mês REABERTO o extrato ao vivo cobra o retardatário (decisão 89) enquanto a lente diária o escondia — Σ dos dias ≠ total do mês, visível na aba, até o refechamento. Agora `dailyRollup` recebe as janelas dos meses FECHADOS e o filtro só se aplica a dias dentro delas; mês nunca fechado, em andamento ou reaberto mostra exatamente o que o extrato cobra. O que a 100 garante segue de pé: num mês FECHADO os dias somam exatamente a fatura congelada (decisão 97), e trace absorvido pelo re-fechamento é faturado, então aparece nos dois lados | Σ diário ≡ extrato ao vivo durante toda a janela reabrir→refechar; `countQuarantined` segue contando só quarentena não resolvida (é essa a pergunta que ele responde) |
| 114 | O limite do scan ao vivo conta com os meses REABERTOS (01/08/2026, re-auditoria da execução, iteração 2 — completa a decisão 112): a guarda de ordem da 112 é necessária mas NÃO suficiente, porque a reabertura auditada (decisão 89) não tem — por projeto — nenhuma guarda de ordem. Reabrir o mais antigo dos meses fechados fazia `firstOpenMonthStart` caminhar a partir do fechado mais antigo AINDA fechado, empurrando o limite para depois do mês reaberto: o dinheiro dele sumia de `/bills`, virava barra de R$ 0,00 na série mensal, e `GET /billing/summary` continuava cobrando (invariante 3). O limite passa a ser o MENOR entre (a) o caminhar para frente e (b) o documento de período NÃO fechado mais antigo — documento de período só existe após ação de ciclo de vida, então `status: 'open'` nele significa exatamente "foi fechado e reaberto". Em defesa em profundidade, a passada de sobras de `/bills` deixou de filtrar por `status === 'closed'`: mês reaberto sem trace no store vira fatura zerada honesta (segue no seletor), e mês reaberto COM traces fora do limite responde ALTO (mesma convenção B-10.2) em vez de sumir | Nenhum mês reaberto pode mais desaparecer da lista nem ser desenhado como R$ 0,00; um limite errado falha ruidosamente em vez de mentir |
| 115 | A isenção de quarentena não resolvida sobre o PENDENTE é a lente do mês FECHADO, e a lente é obrigatória na porta (01/08/2026, re-auditoria da execução, iteração 2 — estende a 113 ao último leitor que ainda aplicava a regra de mês congelado a mês vivo): dentro de um mês congelado o retardatário `pending_price` está fora da fatura por construção (decisão 100) e `countQuarantined` o carrega; em qualquer outro estado — nunca fechado, em andamento e sobretudo REABERTO — o extrato ao vivo cobra aquele mês, então um trace pendente ali é custo ABERTO da mesma fatura: conta, BLOQUEIA o fechamento e precificá-lo é exatamente o fluxo de correção da decisão 89. Sem isso, o re-fechamento de um mês reaberto congelava uma v+1 que omitia o custo do retardatário enquanto reportava zero pendente (invariante 2). `pendingPriceSummary(…, { excludeUnresolvedQuarantine })` passa a EXIGIR a lente na porta e no adaptador — sem default, para que nenhum leitor herde a lente errada em silêncio — e `listBills` recebe as janelas dos meses fechados como `dailyRollup` já recebia, para que `/bills`, `/billing/summary` e `make billing-close` nunca relatem números de pendente diferentes para o mesmo mês | Uma pergunta, uma resposta: os três leitores do pendente concordam em todo status de período; `make billing-close` de mês reaberto só passa depois de precificar o retardatário |
| 116 | O teto de 10.000 vale para as DUAS metades da listagem — e o contrato publicado passa a dizer isso (01/08/2026, re-auditoria da execução, iteração 3 — emenda a 77(b) e registra o que a iteração 2 mudou sem logar): `TOTAL_CAP = MAX_PAGINATION_SKIP` e `total_pages` deriva de `total`, então um total exato sem filtros anunciava 2.500 páginas das quais a guarda de profundidade (decisão 79) servia 500 — a página 501 respondia 400 e o "Próxima" da UI pintava um apagão que não existia; a iteração 2 removeu o `!unfiltered &&` de `mongodb-trace-query-repository.ts` e ficou certo no código. Só que a mudança não chegou a NENHUMA superfície que o cliente lê: a descrição de `GET /api/v1/traces` no OpenAPI, a coleção Postman e a própria 77(b) seguiam prometendo "sem filtros o total é exato", então um integrador que montasse medidor de tamanho do arquivo em `total` (o `openapi.json` é a superfície de integração da decisão 103) ficava cravado em 10.000 para sempre, sem erro e sem sinal além da flag que a documentação dizia ser dispensável ali. Regra vigente, agora escrita nos três lugares: até 10.000 o total é exato e `total_capped` é false; a partir do teto a resposta é `total: 10000`, `total_capped: true` e displays com "+" — e o número honesto de um arquivo maior que o teto é `total_display`, nunca `total` (invariante 6: o arquivo cresce além do teto por projeto), que é exatamente o campo que `deploy-lib.sh#summary_data` e `5-verify-client.sh` já imprimem | `make deploy-smoke` passa a cruzar código × OpenAPI × Postman × log (caso D) e `openapi.spec.ts` fixa a descrição do endpoint: mudar o teto sem mudar a prosa — ou o contrário — quebra alto em vez de divergir em silêncio |
| 117 | Migrar é PASSO do deploy de produção, não folclore de runbook (01/08/2026, re-auditoria da execução, iteração 3): a §"Production deployment" do README era um `docker compose … up -d` e mais nada, e nada migra sozinho — nenhuma imagem, entrypoint ou serviço chama `runMigrations` (a api sobe com o CMD `node dist/main/index.js`, que só faz connect + start; a única porta é `make migrate` ⇒ `dist/main/jobs/run-migrations.js`). Uma stack subida EXATAMENTE como documentado ficava sem índice NENHUM, e aqui os índices carregam correção, não velocidade: o insert-once do ingestor É o índice único de `traceId` (auditoria C-7.3 — sem ele o mesmo trace entra duas vezes, com carimbo próprio e incremento próprio no cubo, e a idempotência prometida do `make sync` deixa de existir — invariante 3) e o 409 de preço duplicado É o E11000 do índice `(model, tokenType, effectiveFrom)` da migração 001 (sem ele a invariante 9 aceita duas versões para a mesma vigência). O README passa a trazer a migração como segundo comando do deploy (forma crua no caminho CI, `make migrate CLIENT=<name>` é o mesmo comando) e a repetir no bloco Day-2, com a regra explícita: no primeiro boot E a cada upgrade de imagem, porque a cadeia é bootstrap de índices (decisão 74) e imagem nova traz migração nova (a 021 da iteração 2 é o caso corrente — deployment existente que só troca a imagem nunca a pega). Rodar migração no ENTRYPOINT foi considerado e NÃO adotado nesta passada: é mudança de comportamento de deploy (a api passaria a escrever schema no boot, em paralelo com o worker, com a falha aparecendo como contêiner unhealthy em vez de linha de runbook) — fica registrada como a alternativa estruturalmente mais forte, a decidir com o dono do deploy | `make deploy-smoke` (caso C) falha se a seção de produção do README perder o passo de migração ou se ele sumir do Day-2 — o mesmo harness que já guarda as URLs sem porta |
| 118 | TODA leitura de porta no deploy passa por `host_port` — inclusive fora dos passos de deploy (01/08/2026, re-auditoria da execução, iteração 3 — completa a correção de portas da iteração 2): `scripts/register-module.sh` lia `API_PORT` com `grep '^API_PORT=' \| cut` e abortava quando a variável estava ausente, que é exatamente a forma de env file que `clients/example.env` convida a usar em host dedicado ("omita as três — os scripts aplicam os defaults do compose"). Resultado: stack saudável servindo em 3000 e módulo NUNCA registrado no module-register (Farol não descobre), com o operador mandado consertar uma variável que o contrato mandou omitir. O script passa a `source scripts/deploy-lib.sh` e a resolver o endpoint por `host_port API_PORT` (valor explícito de `API_PORT` ou de `ENDPOINT` continua vencendo; env file ausente vira erro nomeado, não default silencioso) e ganha `DRY_RUN=1`, que imprime endpoint + manifest e sai antes de tocar o register — é assim que o smoke o exercita sem rede. O lint do `make deploy-smoke` deixa de procurar a FORMA `$(get PORT)` e passa a recusar qualquer leitura de `API_PORT\|LANGWATCH_PORT\|UI_PORT` fora de `host_port` (a variante `grep\|cut` passou batida por ele uma iteração inteira, enquanto a linha final do smoke certificava que nenhum script lia porta crua) | Um env file legalmente sem portas registra o módulo em `http://localhost:3000`; a próxima variante sintática de leitura crua de porta é recusada na origem, não no incidente |
| 119 | O limite do scan ao vivo é ancorado no DADO, não no ciclo de vida (01/08/2026, re-auditoria da execução, iteração 3 — fecha a classe das decisões 112/114): mês que nenhuma ação de ciclo de vida tocou NÃO tem documento de período (`markClosed`/`markReopened` são os únicos escritores), então era invisível às duas metades de `firstOpenMonthStart`. Bastava um backfill documentado — `make sync FROM=… TO=…`, a recuperação de dead-letter do README — cair num mês nunca fechado e ANTERIOR ao fechado mais antigo para o dinheiro dele sumir de `/bills` e da série mensal enquanto `GET /billing/summary` continuava cobrando (invariante 3). A guarda de ordem da 112 não impede isso e o comentário que afirmava impedir estava errado: ela é verificação PONTUAL dentro do `close()`, e a ingestão só põe em quarentena o retardatário cujo PRÓPRIO mês está fechado. O caminhar passa a ancorar no MENOR entre o mês fechado mais antigo e o mês do TRACE ARMAZENADO mais antigo, e `earliestTraceAt` vira parâmetro posicional OBRIGATÓRIO de `firstOpenMonthStart` — sem default, para que nenhum leitor novo herde o buraco em silêncio; `listBills` e a série mensal passam a lê-lo junto com os períodos (o mesmo min indexado que a guarda de fechamento já usava). A metade (b) fica como aperto: só pode puxar o limite para TRÁS, nunca para frente | Propriedade agora enunciada e testada na função: todo mês não fechado com pelo menos um trace começa em ou depois do limite, com ou sem documento de período — as três variantes do mesmo defeito (112, 114 e esta) deixam de admitir uma quarta |
| 120 | O fechamento dobra o mês PÁGINA A PÁGINA — nenhum mês residente em memória (01/08/2026, re-auditoria da execução, iteração 3): a iteração 2 encolheu a TRANSAÇÃO do fechamento e deixou o PROCESSO como parede mais baixa. `fetchUsageRecords(mês)` materializa um documento por trace carimbado e o `make billing-close` roda no contêiner de 512m (heap V8 de 259 MB, medido na própria imagem): a ~200 mil traces o job morre com `FATAL ERROR: Reached heap limit`, SIGABRT — antes de qualquer catch tipado, de forma determinística, então o mês NUNCA fecha e a 112 passa a bloquear todos os seguintes (invariante 8 inalcançável no deployment inteiro). O motor de extrato ganha forma dobrável (`createStatementFold`: `add(record)`/`statement()`/`recordCount()`/`appliedPriceVersions()`), o repositório de snapshot ganha `insertWithPeriodCloseStaged` (o chamador é chamado de volta com `stage` e empurra o mês em janelas diárias) e o fechamento passa a ler um dia, dobrá-lo, encená-lo e soltá-lo: o pico vira uma página + os acumuladores (limitados por chaves distintas, não por traces) + a lista de ids faturados que a reconciliação consome (só strings). `STATEMENT_LOGIC_VERSION` NÃO se move: nenhuma aritmética mudou e o motor é independente de ordem por contrato, então o extrato sai idêntico — continua havendo UMA conta (T7) | Consequência ciente, registrada como remanescente: o caminho de LEITURA ainda materializa o mês pelo mesmo `fetchUsageRecords` (`GET /billing/summary` de mês aberto, e o `/statement` que dele deriva) — mesma parede, agora no contêiner da API; dobrá-lo é o passo natural agora que o fold existe |
| 121 | A área encenada do fechamento vive o tempo da TENTATIVA (01/08/2026, re-auditoria da execução, iteração 3): a fase 1 encena as linhas de uso FORA da transação, sob chave privada da tentativa, e só o publish varria as áreas superadas — os dois desfechos que NÃO publicam (perder a corrida do flip ⇒ `'conflict'`; a recusa tipada por header duplicado) voltavam sem varrer, e a varredura é versionada por chave, então nenhum fechamento POSTERIOR alcançava aquelas linhas: ficavam em `billing_snapshot_usage` para sempre, invisíveis a `findUsageRecords`/`findUsageTraceIds`, inflando coleção e índice único (num mês de 500 mil traces, ~250 MB por corrida perdida). Todo caminho que não publica — inclusive erro levantado pelo chamador no meio do paginado — passa a dropar a PRÓPRIA área antes de retornar/relançar (`discardStaging`, best-effort pelo mesmo motivo da varredura), o que torna verdadeira a promessa do doc da classe: fechamento que não aconteceu não deixa rastro em disco. O teste que se chamava 'writes NOTHING' e só olhava pela indireção do header passa a contar documentos de verdade | Área encenada é privada da tentativa, então dropá-la nunca alcança linhas de outra; o retry idempotente do fechamento segue valendo |
| 122 | Os comparadores do extrato viram ordem TOTAL sobre a própria chave de agrupamento — e a conta única fica legível em diff (01/08/2026, re-auditoria da execução, iteração 4): `compareLines` ordenava por cinco campos enquanto `lineKey` agrupa por SEIS (o preço unitário aplicado entra na chave desde a decisão 90), então duas linhas distintas comparavam IGUAL e o sort estável as deixava na ordem de INSERÇÃO do Map — isto é, na ordem em que os registros chegaram. Como o fechamento dobra o mês por dia (decisão 120) e a leitura ao vivo e o rebuild de reprodutibilidade (T6) leem o mês inteiro por `traceId`, a MESMA linha caía em índices diferentes, e `reconcileDisplayCents` desempata o centavo POR ÍNDICE: a mesma linha saía R$ 0,02 no CSV do mês aberto e R$ 0,01 no snapshot congelado, sem nenhuma mudança de dado, e `lines`/`priceVersionsApplied` do snapshot deixavam de ser reprodutíveis a partir dos próprios inputs congelados (a propriedade de aceite enunciada em `billing-snapshot-model.ts`). `sortPriceVersions` tinha o buraco gêmeo contra a chave de `addRecordPriceVersions`. O gatilho é real, não teórico: a migração 019 (decisão 102) rebaixa `traces.model` e deixa a variante de caixa colidente "como armazenada", então dois carimbos com mesmo (modelo, tipo, vigência) e preços DIFERENTES coexistem sob uma chave canônica. Os dois comparadores ganham o termo do preço como último critério; `STATEMENT_LOGIC_VERSION` deliberadamente NÃO se move — a saída só muda onde a ordem era indefinida (verificado por teste diferencial motor-antigo × motor-novo: 131 fixtures sem empate byte-idênticas, 0 divergências), e bumpar marcaria falsamente todo snapshot congelado como produto de outra conta. No mesmo passe os seis bytes NUL CRUS do arquivo viram o escape `'\u0000'` (mesmo caractere em runtime, zero mudança de comportamento): com eles o git classificava THE billing calculation como blob binário — todo commit que tocou o motor mostrou `Bin … 1 file changed, 0 insertions(+), 0 deletions(-)`, e `grep`/`rg` pulavam o arquivo em silêncio, que foi exatamente como este defeito sobreviveu a quatro ondas de auditoria | A promessa da 120 ("o motor é independente de ordem por contrato") passa a ser propriedade imposta por construção, não suposição: fechamento paginado e leitura ao vivo produzem extratos byte-idênticos, centavos exibidos inclusive, fixados por teste nas DUAS ordens reais (mês inteiro por `traceId` e fold dia a dia). Snapshot congelado ANTES desta linha num mês com empate segue irreprodutível — já era; reabrir e re-fechar o regrava na ordem total |
| 123 | UM parser de data para TODA porta de runbook (01/08/2026, re-auditoria da execução, iteração 5): `run-sync` era a última porta ainda no `new Date()` cru — `--from`/`--to` aceitavam a grafia pt-BR `01/07/2026` como 7 de janeiro (m/d/y americano, em meia-noite LOCAL), então a janela que o operador leu como "julho inteiro" (`--from 01/07/2026 --to 01/08/2026`) sincronizava UM dia de janeiro, com os dois guardas do job (data válida, `from < to`) passando e o processo saindo 0. Pesa duas vezes: `make sync` é a única porta manual de backfill para o arquivo permanente E a porta de recuperação do dead-letter, cujo runbook manda APAGAR a linha depois de re-sincronizar a janela (não há flag de resolvido — linha que existe é trace que falta no arquivo) — janela errada perde o trace do arquivo e da própria trilha de recuperação (invariante 6). A grafia `06/07/2026` é pior: os dois extremos caem em datas reais, a janela sai POPULADA e nem o `fetched 0` da linha final da execução acusa. `parse-effective-from` virou `parse-runbook-date` e serve as DUAS portas — batizar o helper por UMA porta (`--effective-from`, corrigida na iteração 2) foi exatamente o que deixou a outra manter a própria grafia por mais quatro iterações de auditoria. As specs cobrem a porta de sync e fixam a fiação das duas portas em NÍVEL DE FONTE (são scripts top-level-await, que conectam o banco no import e não dão para exercitar em processo — mesma técnica de `architecture-boundaries`): devolver `new Date(...)` a qualquer uma delas quebra exatamente um teste | Job novo que receba data usa `parseRunbookDate`, nunca `new Date()` — a regra de grafia é do helper, não de cada porta |
| 124 | O pacote se chama `module`, não `api` (03/08/2026, pedido do usuário): `packages/api` virou `packages/module` e o campo `name` do `package.json` foi de `api` para `module` — o repositório é o **Module de Tracing** do Khal OS, e o diretório era a última peça que ainda o chamava de "a API". Renomeados juntos os apontadores de caminho (`docker/api.Dockerfile` incluindo `--workspace=module`, montagem de fixtures em `compose.dev.yml`, `scripts/` de bump/seed/smoke-test/loadtest, `.claude/launch.json`, README e docs vivos) e as duas entradas do `package-lock.json`. NÃO mudaram — e a distinção é o ponto: o prefixo HTTP `/api/v1` (contrato público), o serviço `api` do compose, a imagem `platform-api:local`, o container `<cliente>-api`, o arquivo `docker/api.Dockerfile` e a env `API_IMAGE` — mexer neles quebraria `.env` de cliente e nomes de container já em pé, sem ganho de clareza. As linhas 53 e 69 deste log guardam a grafia antiga de propósito: são registro do que foi decidido na época **Parcialmente superseded pela decisão 125 (03/08/2026)**: a imagem passou a ser `platform-module:local`, `docker/api.Dockerfile` virou `docker/module.Dockerfile` + `docker/connector.Dockerfile` + `docker/ui.Dockerfile`, e `API_IMAGE` virou `MODULE_IMAGE`/`CONNECTOR_IMAGE`. Seguem intactos o prefixo HTTP `/api/v1`, o serviço `api` do compose e o container `<cliente>-api`. | Caminho novo é `packages/module/...`; o que fala HTTP ou docker continua `api` |
| 125 | Split de pacotes: o módulo só vê trace já ingerido (03/08/2026, pedido do usuário): o workspace vira `@observability/core` + `@observability/module` + `@observability/connector` + `@observability/ui` — core é o fundo do grafo (domínio, carimbo de preço, reprocess, TODOS os repositórios Mongo, migrações, a régua única de data `iso-date-rule`); module é a API de leitura (traces/sessions/billing/preços/ciclo de mês), **vendor-blind por pacote** (nenhum `langwatch` no src inteiro, regra que antes excetuava o diretório do adapter); connector é o lado da fonte (adapters LangWatch ClickHouse/HTTP, fake+fixtures, syncTraces/syncBatches, repositórios de bookkeeping da ingestão, worker + `run-sync`). Grafo `module → core ← connector`, nunca module ↔ connector em produção — `@observability/connector` entra no module como devDependency SÓ para testes (harness de rotas semeia pela ingestão real; teste de pipeline prova o invariante 3 fim a fim), confinamento garantido por teste de arquitetura. O carimbo e o reprocess ficam no CORE porque as duas portas de re-carimbo (POST /prices imediato, varredura do worker) vivem em pacotes diferentes — uma conta só (invariante 3) por construção. A decisão 123 sobrevive invertendo a direção: a régua de data vira UMA schema zod no core e as três portas a consomem; os pins de fiação em nível de fonte moram no pacote dono de cada porta. Docker espelha: `platform-module` (vendor-free por construção — verificado com grep na imagem) + `platform-connector` (worker + fixtures); contrato de env muda `API_IMAGE` → `MODULE_IMAGE` + `CONNECTOR_IMAGE`, o `SERVER_PORT` falso do worker morre, `make sync` roda no serviço trace-ingestion-worker. Consumo entre pacotes é por workspaces npm; linguagem ubíqua (03/08/2026): este repositório é o COMPONENTE `observability` do Khal OS e componente = module + connector — daí o escopo `@observability/` (que também evita colisão com pacotes reais do registro: `module`/`core`/`connector` existem no npmjs), exports map para dist, `tsc -b` com project references, jest mapeia `@observability/*` para src | Código de fonte/ingestão novo vai no connector; regra de negócio compartilhada vai no core; o module nunca importa connector em produção |
| 126 | Register vira Catalog e o M2M perde os scopes de vez (03/08/2026, decisão do dono na sessão de brainstorm dos happy paths): todo register da plataforma passa a se chamar **Catalog** (Module/Connector/Agent/App Catalog) e a autenticação M2M é SÓ identidade — token válido + tenant certo, sem scopes (o claim `scope` fica opcional e ignorado, para reintrodução aditiva). Nos scripts deste repo: `register-module.sh` e `connector/register.sh` ganham `CATALOG_URL` (grafia legada `REGISTER_URL` segue honrada), token em três precedências (TOKEN explícito → sessão do M2M Auth System via `client_credentials` com `AUTH_SYSTEM_URL`+`M2M_CLIENT_ID/SECRET`, sem renew: expirou, pede outra → dev claims SEM scope) e fallback LEGACY-COMPAT: se o catalog local ainda responder 403 por guardar rota com scope, UMA retentativa com os claims escopados avisando no stderr — apagar quando a plataforma remover os guards. O manifesto do module declara `auth.requiredScopes: []` e ganha `capabilities` com a tupla completa de intenção (signal `monitoring.trace`, operation `read`, transport `http`, protocol `rest`, protocolVersion `v1`, encoding `json`); o binding do connector LangWatch declara `protocolVersion: "1.0"`, casando com o intent que o agent passa a enviar. O fluxo alvo: quem fala com o catalog no deploy é o CD com a identidade M2M do artefato ("CD agindo como o module") — o processo do module em runtime nunca fala com o catalog | Script novo de registro nasce sem scope e com os três degraus de token; a plataforma (khal-platform) ainda não renomeou os serviços — os defaults de porta e os nomes `@khal/*-register` seguem válidos até lá |
| 127 | ClickHouse é a ÚNICA fonte real de traces — a ingestão HTTP morre (04/08/2026, decisão do usuário durante a revisão da auditoria pós-split): nenhum cliente jamais ingerirá via HTTP, então o `HttpLangWatchClient` (+ schema/mapper/specs) foi REMOVIDO — supersede a metade "HTTP como fallback" da resolução da QA14 e o caminho LangWatch-externo da decisão 78 (ressuscitável do git se um dia existir cliente SaaS). A seleção de fonte passa a ser DECLARADA, nunca inferida: o fake de fixtures só entra com `TRACE_SOURCE=fixtures` explícito no env do cliente (ou sob jest), e SEM fonte configurada o sync QUEBRA em vez de adivinhar — a cadeia antiga terminava em `new FakeTraceSourceClient()`, e UMA env vazia fazia `make sync` "ingerir" as 9 fixtures de demo no arquivo permanente de um cliente real, carimbadas e faturadas, com exit 0 (auditoria pós-split, A-1). O gate de onboarding do ClickHouse muda de `LANGWATCH_API_KEY` para `LANGWATCH_PROJECT_ID` (o valor que as queries realmente filtram), a `LANGWATCH_API_KEY` deixa de chegar a QUALQUER contêiner do componente (é credencial dos AGENTES, entregue via vault — fica no env file só como hand-off do onboarding), e o fragmento `api:` do compose.connector.yml morre com ela: o module consome trace já ingerido e não sabe NADA de LangWatch, que é o resultado da decisão 125 levado até o compose. Cada branch do factory LOGA a fonte escolhida; `sync-factory.spec.ts` pina seleção, opt-in e crash (falha se a cadeia inferida voltar). [Emenda no mesmo dia: a `LANGWATCH_API_KEY` sai do CONTRATO env por inteiro — nem placeholder; a key mora só no Postgres do próprio LangWatch, o push de demo a busca de lá na hora (`lw_project_key` em deploy-lib.sh), operador copia da UI para o vault, e `--langwatch-key` deixa de existir em 1-init/deploy-demo. O marcador de idempotência do 3-onboard vira o PROJECT_ID.] | Uma env vazia deixa de poder fabricar dados de faturamento; a fronteira module⊥connector vale também para credenciais |
| 128 | Trace com MODELO mas zero tokens medidos vira `no_measured_usage` (04/08/2026, decisão do usuário — resolve a Q2/A-4 residual da auditoria pós-split): o modelo do trace é DERIVADO dos spans LLM, então modelo presente + zero tokens em toda parte (depois do fallback A-2 por spans) significa que um LLM rodou e o uso não foi reportado — custo DESCONHECIDO, não zero; carimbar R$ 0,00 seria a mentira que a invariante 2 proíbe. Status terminal por imutabilidade (contagens nunca mudam; insert-once), nunca precificado, NÃO bloqueia o fechamento (não existe preço cujo registro o resolveria — diferente de pending_price), contado no summary como campo de CONTEXTO vivo (como quarantined_trace_count; nunca input do statement — STATEMENT_LOGIC_VERSION intocado). Trace SEM modelo e sem tokens continua carimbado R$ 0,00: trabalho tool-only é honestamente gratuito. Gate aplicado no stampTokens (`hasModel`), fluindo por ingestão e reprocess (inalcançável do conjunto pending — pending exige token usado). Escopo: contagem só no summary (não em /bills nem no read-model de sessões — o custo de sessão já exclui não-carimbados corretamente). | Todo R$ 0,00 carimbado passa a ser comprovadamente um trace sem trabalho de LLM; lacunas de instrumentação ficam visíveis sem congelar o faturamento |
| 129 | O serviço compose continua `api` — DELIBERADO, não resquício (04/08/2026, decisão do usuário — resolve a Q3/metade externa do H-3 da auditoria pós-split): `api` nomeia o PAPEL (o que serve `/api/v1` — o nome que operadores digitam: `docker logs <cliente>-api`, healthchecks, runbooks), enquanto `module`/`platform-module` nomeiam o PACOTE/imagem (do que ele é feito). Renomear recriaria contêineres em todo deployment por simetria cosmética. Mesma divisão já anotada na decisão 124. | Auditores futuros não devem re-apontar a assimetria serviço `api` × pacote `module`; ela é intencional |
| 130 | O relógio do cliente: `CLIENT_TIMEZONE` (IANA, OBRIGATÓRIO) define fronteira de faturamento E fuso de exibição (04/08/2026, decisão do usuário — resolve a B-4/Q1 da auditoria pós-split): um knob só por deployment (single-tenant, invariante 5), declarado nunca inferido (sem fallback — fuso errado é fatura errada, mesma filosofia da decisão 127). O sistema é independente do visitante: o servidor formata exibição no fuso do CLIENTE, então duas pessoas em países diferentes veem telas idênticas que batem com a fatura. Offsets por instante via Intl (DST-correto para qualquer zona IANA; $dateTrunc recebe o nome da zona). Sites: monthWindow (ex-monthWindowUtc), monthKeyOf/quarentena/reprocess, resolvePeriodStatus, rollup diário, dia do cubo de facetas (clientDayOf, ex-utcDayOf), escada do daily lens, projeção, pager do fechamento, formatters. O snapshot GRAVA o fuso em que foi cortado e um re-close sob fuso diferente é BLOQUEADO — mudança de fuso é forward-only, nunca sobre mês fechado. Datas de vigência de PREÇO seguem instantes UTC (QA19 pendente — deliberadamente fora desta decisão). UI declara o fuso no cabeçalho via /client.json. | Fim da divergência B-4 (últimas 3h locais de cada dia/mês caíam no balde errado); trocar a fonte do valor (env → catálogo da plataforma) fica barato porque a história carrega o próprio fuso |
| 131 | Fechamento automático de mês vira sidecar OPT-IN — refina a QA4, responde a QA5 (04/08/2026, decisão do usuário): novo serviço compose `billing-close-scheduler` (imagem do módulo, réplica única por `container_name`, molde do trace-ingestion-worker — loop reconciliador, NÃO cron, decisão 60), que só EXISTE quando o env do cliente ativa o perfil `billing-auto-close` via `COMPOSE_PROFILES` (declarado, nunca inferido — knob ausente, contêiner ausente). A cada `BILLING_AUTO_CLOSE_CHECK_INTERVAL_SECONDS` (default 900) o loop caminha os meses-calendário do cliente a partir do trace mais antigo e fecha, do mais antigo para o mais novo, todo mês cujo fim (meia-noite do CLIENTE, decisão 130) passou há pelo menos `BILLING_AUTO_CLOSE_DELAY_MINUTES` (default 60 — o store atrasa ~16min do vivo pela quarentena de ingestão + poll, decisões 60/61; fechar em cima da meia-noite só enche a quarentena da decisão 100 com retardatários; a correção nunca depende do delay). UM use case de fechamento para as duas portas (decisão 87 intacta — job, nunca endpoint HTTP): o gatilho vira propriedade da composição (`trigger: 'runbook' \| 'scheduled'`), honesto no snapshot E na auditoria do período. Mês bloqueado por pending_price re-tenta a cada ciclo listando os modelos sem preço — a QA5 está respondida: a fatura ESPERA, nunca decisão forçada (registrar o preço já re-carimba os pendentes, decisão 57, e o ciclo seguinte fecha sem segundo passo manual). Mês REABERTO NUNCA re-fecha sozinho (e segura tudo que é mais novo, pela ordem mais-antigo-primeiro da decisão 112): o fluxo de correção da decisão 89 pertence a quem reabriu. Mês vazio nunca fecha (mesma isenção do guard de ordem); mês já fechado é pré-checado, não erro; corrida com o runbook é benigna (update guardado + índice único — um vencedor). Stdout continua sendo a notificação US5; heartbeat de PROGRESSO (`/tmp/billing-close-heartbeat`, semântica G-1) é a liveness. | O fechamento deixa de depender de alguém lembrar do dia 1, sem abrir mão de nada: mesma porta, mesma auditoria (agora assinada), mesmos bloqueios — e o runbook continua existindo, idêntico, para exceções e reaberturas |
| 132 | Superfície canônica de env da plataforma: o quarteto KHAL_* (07/08/2026, decisão do usuário — nomes iguais para coisas iguais em todos os consumidores do Khal OS): a config operador-facing do módulo passa a ser `KHAL_DISCOVERY_URL` + `KHAL_TENANT` + `KHAL_CLIENT_ID` + `KHAL_CLIENT_SECRET` — a MESMA superfície do agent-template. A URL do Auth System deixa de ser configurada direto: é RESOLVIDA em runtime de `GET {discovery}/.well-known/registers?tenant=…` (ADR-97 da plataforma; cache pelo Cache-Control, max-age=300), então mover o Auth System não toca env de cliente. FAIL CLOSED por papel de servidor (diferente do agente, fail-open por ser telemetria): discovery sem resposta ⇒ introspecção vira erro não-cacheado ⇒ 401; URL last-known É servida durante refresh falho (stale vence nada). Grafias antigas seguem honradas como aliases depreciados: `AUTH_SYSTEM_CLIENT_*` = a mesma credencial (KHAL_* vence quando ambas existem); `AUTH_SYSTEM_URL` direto continua válido SÓ sem discovery configurado (com discovery, é ignorado com log — nunca meio-a-meio). Decisões 84/103 intactas: gate por env, authenticated-or-not only, docs/healthcheck abertos. | Um FDE aprende UMA superfície de env para qualquer componente do Khal OS; a plataforma ganha liberdade de mover serviços sem redeploy de consumidor |
| 133 | Sem aliases antes do primeiro deploy (07/08/2026, decisão do usuário — limpeza pré-prod): TODAS as grafias legadas de env caem. O module só lê o quarteto KHAL_* — AUTH_SYSTEM_URL e AUTH_SYSTEM_CLIENT_* saem do schema, do compose.module.yml, dos exemplos e das mensagens; auth vira discovery-only (KHAL_DISCOVERY_URL unset ⇒ API aberta, PoC). Os scripts de registro só leem KHAL_TENANT, KHAL_CLIENT_ID/SECRET e MODULE_/CONNECTOR_CATALOG_URL — CATALOG_URL, REGISTER_URL, TENANT e M2M_CLIENT_* removidas. Racional: nada disso chegou a produção, então remover custa zero hoje; cada alias mantido viraria contrato eterno. De quebra, o SCRUB do Makefile passa a listar o quarteto KHAL_* (estava faltando — um KHAL_* exportado no shell do operador vazaria por cima dos env files de TODOS os clientes) | Env de cliente que use AUTH_SYSTEM_* simplesmente não ativa auth (boot aberto, PoC) — rodar scripts/5-verify-client.sh, que agora confere KHAL_DISCOVERY_URL; docs/README/openapi atualizados |
| 134 | Logging estruturado com port próprio e knobs de ambiente (08/08/2026, pedido do usuário): nasce o port `Logger` em `@observability/core/common/logging` (níveis trace/debug/info/warn/error/fatal + `silent`, `child(bindings)`, campos estruturados) com UMA implementação própria zero-dependência (`createLogger` — linhas JSON no stdout por default, formato `pretty` para o loop de dev; erros serializados com stack, ciclos cortados, uma chamada de log NUNCA lança). Knobs `LOG_LEVEL`/`LOG_FORMAT` lidos por UM leitor compartilhado no core (`parse-log-env`, mesmo molde C-6 do Mongo): default info/json em produção, pretty em development, SILENT em test (o logger escreve direto no stdout e furaria a captura do jest; suíte que quer log seta `LOG_LEVEL` explícito); knob inválido derruba o boot (F-4), e a própria falha de env sai como linha estruturada via leitor bootstrap tolerante. Injeção respeita as camadas: use cases e repositórios recebem `logger` opcional via args-object (default `nullLogger` — fábricas SEMPRE injetam o real via `makeLogger` por imagem, `service: module\|connector` + `component` como bindings); `MongoDb` (singleton por processo) ganha `useLogger` com o mesmo dono do connect/disconnect; daemons (worker de ingestão, scheduler de fechamento) falam JSON estruturado; jobs one-shot de runbook mantêm console DELIBERADAMENTE (saída de operador, não log de aplicação — exceção codificada no lint). Testes trocam spy de console por `RecordingLogger` injetado (logging-test-fakes). Compose repassa `${LOG_LEVEL:-}`/`${LOG_FORMAT:-}` nas três imagens | Nível de log muda por env sem rebuild; `docker logs \| jq` funciona em toda linha; trocar por um backend pesado (pino etc.) é mudança de composition root — o port é o contrato |
| 135 | CI e portões de commit/push (08/08/2026, pedido do usuário): ESLint 9 flat config ÚNICO na raiz (typescript-eslint + prettier config; `no-console: error` em todo `packages/*/src` EXCETO `main/jobs/**` — a superfície CLI de runbook; eqeqeq com `null: ignore`, no-explicit-any) + Prettier (singleQuote, repo inteiro formatado uma vez; YAML/ops/docs fora do escopo por `.prettierignore`). Husky: pre-commit roda lint-staged (eslint --fix + prettier só nos staged), commit-msg roda commitlint conventional (o formato que o histórico já usava), pre-push roda typecheck + lint + format:check + unit suites (só `*.spec.ts`, sem Mongo — rápido). GitHub Actions (`.github/workflows/ci.yml`): jobs paralelos lint/format, typecheck (build antes — o exports map resolve dist), test (`npm run test:ci` = build + packaging-check + unit+integration com o ratchet de cobertura E-5; binário do mongod cacheado) e commitlint no PR inteiro. Os hooks locais são a cópia rápida; o workflow é a autoridade | Commit fora do padrão ou código fora do lint nem entra na história; push quebrado é barrado na máquina de quem empurra, e o CI repete tudo com as suítes de integração por cima |
| 136 | A plataforma alcançou o modelo sem scopes — os fallbacks LEGACY morreram (04/08/2026): a khal-platform (branch feat/m2m-identity-module-lifecycle) trocou `guardScope` por `guardTenant` em todos os registers (token válido + tenant, claim `scope` ignorado), o Auth System aceita `ownerType: module`, o Module Catalog provisiona a credencial M2M no primeiro PUT (`clientSecret` entregue UMA vez, molde do agent-register), o manifesto NASCE `desativado` e ganhou `POST /modules/{id}/activate` (resolução/lista só servem `ativo`), e os catalogs podem delegar validação por introspection (env-gated: `AUTH_SYSTEM_URL` + credencial própria). Nos scripts deste repo: caiu a retentativa LEGACY com claims escopados, o endpoint de sessão corrigiu para `/oauth/token` (era `/token` — nunca existiu), e o `register-module.sh` agora ATIVA o module após o PUT (`SKIP_ACTIVATE=1` mantém desativado). Provado vivo: registro→credencial→ativação, re-registro via sessão RS256 real do Auth System, e o gate de introspection recusando dev claims e aceitando sessão real | Registro local imprime a credencial M2M do module UMA vez — guarde; o dev token continua sendo `{tenant, client_id}` sem scope *[Renumerada de 130 para 136 em 09/08/2026 — o número colidia com "O relógio do cliente"; colisão detectada pelo spec-check]* |
| 137 | O workflow spec-driven vira ferramenta executável: skills + portão de integridade (09/08/2026, pedido do usuário — preparação para extrair o module-template): oito skills de projeto em `.claude/skills/` codificam os fluxos que até aqui viviam só em convenção — `append-decision` (número = max+1, anotação de supersessão, nunca editar linha antiga), `resolve-qa` (RESOLVIDA + riscado + marcadores `// QAnn:`), `feature-slice` (a fatia vertical de ~12 arquivos, critérios de pronto primeiro), `env-knob` (o checklist de 8 estações, SCRUB em destaque — esquecido nas decisões 78/133), `runbook-job`, `spec-review`, `author-story` e `spec-kickoff` (substituto vivo do kickoff-prompt.md no template). Todas descobrem caminhos/números em runtime (glob do backlog, max-scan, exemplares por semelhança) — zero versão/escopo hardcoded, portáveis para o template como estão. O que uma skill não enxerga (branch irmã reivindicando o mesmo número) vira portão mecânico: `scripts/spec-check.mjs` (zero deps, no CI e no pre-push) valida numeração única/ascendente do log, referências RESOLVIDA, marcadores decision/QA do código e completude do SCRUB. Na primeira execução o portão pagou a si mesmo: pegou LOG_LEVEL/LOG_FORMAT fora do SCRUB (a mesma classe das decisões 78/133) e um 130 DUPLICADO no próprio log (renumerado para 136), além da região 124–131 fora de ordem (reordenada, conteúdo intacto) | O processo deixa de depender de disciplina de leitura do CLAUDE.md: quem pedir "registra a decisão" ou "adiciona um env var" cai na skill certa, e a classe de colisão 132/133 é impossível de mergear. O module-template herda o sistema inteiro copiando `.claude/skills/` + `scripts/spec-check.mjs` |
| 138 | Regra do carimbo CONFIRMADA: preço vigente na DATA DO TRACE, nunca na data da ingestão (09/08/2026, decisão do usuário — resolve a QA19): o carimbo usa a versão de preço efetiva em `trace.startedAt`, resolvida as-of no ato da escrita — exatamente o default que o código já implementava e os testes já pinavam (`trace-ingestor.ts`, `reprocess-pending-db-use-case.ts`, `derive-pending-price.ts`). Trace de 31/07 ingerido em 01/08 mantém o preço de julho: correção contratual preservada, backfill determinístico (re-rodar a mesma janela em momentos diferentes produz a mesma fatura), imutabilidade intacta (invariante 1 perde o "pending"). A borda deixada pela decisão 130 fica como está POR ORA: `effective_from` de preço segue comparação de INSTANTES UTC contra `startedAt` (a janela de ~3h entre a meia-noite UTC e a meia-noite do cliente usa o preço novo) — comportamento implementado e agora documentado; mover a efetividade de preço para o fuso do cliente seria decisão nova, não correção. Os marcadores `// QA19:` passam a apontar onde a resposta VIVE, não trabalho pendente (mesma virada da QA14/decisão 40) | Zero mudança de código — a confirmação congela o default como regra. Todo carimbo já gravado está correto por construção; a QA aberta que mais tocava o código está fechada |
| 139 | Contrato de env fail-fast: capacidade e identidade são declaradas, nunca defaultadas (10/08/2026, pedido do usuário — preparação do deploy AWS): `MONGO_DB_NAME` vira `MONGO_USAGE_DB_NAME` e é OBRIGATÓRIO — o fallback silencioso `:-${CLIENT_NAME}` dos três compose files morreu (em cluster com múltiplos databases do cliente, nome inferido roteia o archive errado); required no reader compartilhado (`parse-mongo-env.ts`, `min(1)` — `''` falha como ausente, campo interno `mongoDbName` mantido) E guard `:?` nos três sites compose. Os cinco knobs de capacidade do LangWatch (`LANGWATCH_WORKERS_REPLICAS`, `LANGWATCH_MEMORY_LIMIT`, `LW_REDIS_MEMORY_LIMIT`, `LW_CLICKHOUSE_MEMORY_LIMIT`, `LW_CLICKHOUSE_CPU_LIMIT`) ganham guard `:?` sem default — o incidente de 1000 traces/s rodou limites dev que ninguém escolheu (fila redis 512m crescendo até OOM, 1 worker drenando; sintoma: lista de traces carregando eternamente = reads famintos sob write flood). Dev continua one-command: `scripts/1-init-client-env.sh` emite os valores dev; `clients/example.development.env` os marca [REQUIRED even locally]; SCRUB do Makefile renomeado junto | Subir uma stack agora exige decisão explícita de capacidade e do nome do store — config ausente é crash NOMEADO no boot, nunca produção em forma dev; contrato quebra envs existentes (a VM Hetzner precisaria setar os seis knobs — moot: decisão 140 descarta aquela stack) |
| 140 | Deploy de produção vira FÁBRICA de tenants na AWS (10/08/2026, decisão do usuário — plano aprovado; ~10 deployments single-tenant no próximo mês): módulo Terraform `tenant/` sobre fundação compartilhada (1 VPC/NAT, 1 ALB com cert wildcard, 1 cluster ECS, 1 ECR em `deploy/terraform/`); por cliente: module API (Fargate atrás do ALB, healthcheck `/api/v1/docs` — decisão 103; auto-scaling min 1/max 4), connector (Fargate singleton, deploy stop-then-start — catch-up idempotente apaga o gap), scheduler de fechamento (o daemon existente como serviço; tick one-shot EventBridge fica como limpeza pós-migração), backup mongodump→S3 agendado COM alarme (no tier Flex/M0 é o ÚNICO backup), LangWatch+ClickHouse em UMA EC2 própria por cliente (stack compose pinada, burst-hardened, alarme de profundidade de fila) e projeto+cluster Atlas PRÓPRIO por cliente (blast radius de burst isolado + restore point-in-time por cliente; Flex→M10 conforme volume — cluster compartilhado é só fallback de orçamento). Imagens imutáveis por SHA (GitHub Actions→ECR via OIDC), config/segredos em SSM/Secrets Manager (os tags [SECRET→SM] do `example.production.env` viram reais) — a classe de incidente rsync-clobber morre por construção. Auth interina: knob `BASIC_AUTH_*` no próprio module até o quarteto `KHAL_*` ter infra real. Gate de aceite: `loadtest/` a 1000 traces/s (fila cresce, drena, ZERO perda; UI lenta durante pico é aceitável) ANTES do cutover. Hetzner era PoC descartável — namastex sobe LIMPO como tenant #1, sem migração | Lambda/API GW e DocumentDB avaliados e REJEITADOS (connector é worker pull, não API; cap de 15min mata backfill de ~49 dias; DocumentDB não tem `$merge`/`collation`/`wildcard`/`$dateTrunc`/`$facet` e os testes de integração validariam engine diferente da produção); custo ≈ $1.0–1.7k/mês para 10 tenants (termo dominante: a EC2 LangWatch por cliente); caminho documentado para escala sustentada: split Helm do LangWatch + dedicated ClickHouse + sync paralelizado |
| 141 | Gate interino de borda no PRÓPRIO module: HTTP Basic env-gated (10/08/2026, plano da decisão 140): knobs `BASIC_AUTH_USER`/`BASIC_AUTH_PASSWORD` — os DOIS juntos ou nenhum, e NUNCA junto com `KHAL_DISCOVERY_URL` (superRefine no schema recusa o boot: meia credencial não meio-habilita auth, e dois gates simultâneos escondem qual 401 respondeu; trocar para a plataforma = DELETAR o par). Middleware `basic-auth.ts` espelha o gate bearer (passthrough sem credencial, bypass de OPTIONS, 401 com challenge `WWW-Authenticate` — navegador pede login), comparação em tempo constante (digests sha256 + timingSafeEqual, `&` não-curto-circuito), split no PRIMEIRO `:` (RFC 7617 permite `:` na senha). Docs continuam abertas (decisão 103 — healthcheck). Na fábrica AWS: tfvars `enable_basic_auth` OBRIGATÓRIO (decisão 139 — API pública aberta é decisão declarada, nunca omissão) e o par vive no secret JSON do tenant | Substitui o basicauth do Caddy da era VM (o ALB não faz Basic) sem segunda borda; a saída do interino é um ato explícito e auditável — flag + delete do par no secret — em vez de um bloco esquecido num Caddyfile; enquanto isso Farol/demos autenticam com `Authorization: Basic` direto no module |

## Registro de questões abertas

| ID | Área | Questão | Por que importa |
|---|---|---|---|
| QA1 | Ingestão | ~~Frequência do sync — diário basta, ou intradiário?~~ **RESOLVIDA (23/07/2026, decisões 59–61)**: sync contínuo — o sidecar `sync-worker` alcança o tip e re-verifica a cada `SYNC_INTERVAL_SECONDS` (default 60s); frescor efetivo ≈ quarentena de 15 min + intervalo. A pressão dos 49 dias também relaxa via override de retenção no onboarding (decisão 64) | Resolvida pela ingestão contínua (T2 forma contínua) |
| QA4 | Fechamento | ~~Gatilho do fechamento: automático no dia 1 ou pelo admin?~~ **RESOLVIDA (31/07/2026, decisão 87)**: pelo admin, via runbook (`make billing-close`) — sem gatilho automático na v1; a saída do job é a notificação da US5. *[**Refinada pela decisão 131 (04/08/2026)**: o gatilho automático passa a existir como OPT-IN explícito (perfil compose `billing-auto-close`) — mesma porta de job, trigger auditado como `'scheduled'`; o default continua runbook]* | A tendência registrada se confirmou |
| QA5 | Fechamento | ~~Fechamento bloqueado: a fatura espera, ou há prazo com decisão forçada?~~ **RESOLVIDA (04/08/2026, decisão 131)**: a fatura ESPERA — o scheduler re-tenta a cada ciclo listando os modelos sem preço, e fecha sozinho no ciclo seguinte ao registro do preço; nunca há decisão forçada nem custo chutado | O SLA operacional entre fim do mês e emissão |
| QA6 | Preços | Modelo novo aparece antes do preço acordado: retém como "sem preço" ou cobra um padrão? | v1 assume reter; mexe no timing de receita |
| QA7 | Composição | ~~Escrita de cache: parte da economia ou custo simples no contrafactual?~~ **RESOLVIDA (31/07/2026, decisão 91)**: EXPLÍCITA — o contrafactual cobre só cache_read; a escrita de cache é custo real mostrado à parte; economia líquida = bruta − escrita (pode ser negativa) | Metodologia do T9 documentada na própria resposta |
| QA8 | Preços | Preço é por modelo apenas (não por agente) — confirmar | Mantém o T4 simples |
| QA9 | Preços | Quem tem acesso ao banco para mexer em preço; confiança + versões imutáveis bastam? | Governança do dado mais sensível do sistema |
| QA10 | Billing | A fatura real soma impostos/itens depois do total da plataforma? | O extrato precisa dizer o que cobre ("total de serviços antes de impostos") — mantida aberta por decisão |
| QA13 | Exportação | ~~Exportar mês em andamento: com marca d'água ou bloqueado?~~ **RESOLVIDA (31/07/2026, decisão 94)**: exporta COM marca d'água PARCIAL (coluna e filename no CSV; marca atravessada no HTML imprimível) | Rota consolidada depois pela decisão 98; a marca d'água segue valendo |
| QA14 | Ingestão | ~~A API do LangWatch entrega spans e payloads com fidelidade? Limites de paginação e tamanho?~~ **RESOLVIDA (spike em 20/07/2026 contra a instância self-hosted)**: `POST /api/traces/search` pagina por pageSize/pageOffset com `totalHits`, mas devolve spans VAZIOS — o detalhe (`GET /api/traces/{id}?format=json`) é obrigatório por trace (N+1; ok no volume atual, rever com QA15). Timestamps em epoch ms; trace NÃO tem finished_at (deriva de total_time_ms). Tokens de cache só em metadata reservada (strings) e/ou métricas de span. Sessão = `thread_id`; agent/channel/domain/subdomain via convenção de metadata (a formalizar com os times). Payloads chegam como enviados (inclusive já mascarados pelo emissor). **Novo ponto aberto derivado: traces multi-modelo existem (agentes claude-code) — v1 os mantém como pending/não classificado em vez de precificar errado; precificação por span é decisão de produto pós-PoC** | Spike concluído; cliente real implementado atrás da mesma interface |
| QA15 | Armazenamento | Estimativa de volume: traces/dia × tamanho de payload → dimensionamento e classe de storage para conteúdo | Arquitetura do T3; metas de desempenho do T10 |
| QA16 | Ingestão | Confirmar a retenção exata do LangWatch (49 dias? igual para todo tipo de dado?) | É o prazo fatal do qual toda a proteção do sync deriva |
| QA17 | Sessions | Semântica de período da sessão: pelo horário de início (proposta) — confirmar | Comportamento do filtro da US21 na virada do mês |
| QA18 | Traces | Conversas/payloads muito longos: regra de truncamento/carregamento sob demanda nos drawers | UX e desempenho da US19/US22 |
| QA19 | Preços | ~~Regra do carimbo: preço vigente na **data do trace** (proposta) vs. no momento do sync — confirmar~~ **RESOLVIDA (09/08/2026, decisão 138)**: preço vigente na DATA DO TRACE (`startedAt`), as-of na escrita — o default implementado virou regra; efetividade de preço segue instantes UTC (borda da decisão 130 documentada como está) | A proposta se confirmou: trace de 31/07 ingerido em 01/08 mantém o preço de julho — correção contratual e imutabilidade preservadas, backfill determinístico |

*(As QA2/QA3 da v1 — escopo do backfill e alcance da API — foram resolvidas pela decisão 21: backfill = o que ainda estiver dentro da janela de ~49 dias no lançamento.)*
