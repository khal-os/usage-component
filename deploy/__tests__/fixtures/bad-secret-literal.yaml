# INVALIDA: senha literal no values. Tem de ser reprovada pela values.schema.json
# (credencial vem do Secrets Manager por ExternalSecret, nunca do git).
services:
  api:
    envOverride:
      MONGO_DB_PASSWORD: "nao-deveria-passar"
